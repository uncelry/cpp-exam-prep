Разбираем тему подробно.

---

## 🔹 How to create and use two-dimensional/multidimensional dynamic arrays

### 🔸 Перевод темы

**Как создавать и использовать двумерные и многомерные динамические массивы**

---

## 1️⃣ Основная идея

**Двумерный динамический массив** — это массив массивов, **размеры которого задаются во время выполнения программы**.

- В памяти он создаётся в **куче (heap)**
    
- Нужно **выделять память для каждой строки отдельно** или использовать **один большой блок**
    
- После использования память обязательно нужно **освободить**
    

---

## 2️⃣ Способ 1: Массив указателей на строки

```cpp
#include <iostream>
int main() {
    int rows = 3, cols = 4;

    // создаём массив указателей на строки
    int** matrix = new int*[rows];

    // выделяем память для каждой строки
    for(int i = 0; i < rows; ++i) {
        matrix[i] = new int[cols];
    }

    // инициализация
    for(int i = 0; i < rows; ++i)
        for(int j = 0; j < cols; ++j)
            matrix[i][j] = i * cols + j + 1;

    // вывод
    for(int i = 0; i < rows; ++i) {
        for(int j = 0; j < cols; ++j)
            std::cout << matrix[i][j] << " ";
        std::cout << std::endl;
    }

    // освобождение памяти
    for(int i = 0; i < rows; ++i)
        delete[] matrix[i];
    delete[] matrix;

    return 0;
}
```

### 🔹 Объяснение

- `matrix` — указатель на массив указателей
    
- Каждый `matrix[i]` — указатель на динамический массив элементов
    
- Память освобождается **в обратном порядке**: сначала строки, потом массив указателей
    

---

## 3️⃣ Способ 2: Один большой блок памяти

```cpp
#include <iostream>
int main() {
    int rows = 3, cols = 4;

    int* matrix = new int[rows * cols]; // один большой блок

    // инициализация
    for(int i = 0; i < rows; ++i)
        for(int j = 0; j < cols; ++j)
            matrix[i * cols + j] = i * cols + j + 1;

    // вывод
    for(int i = 0; i < rows; ++i) {
        for(int j = 0; j < cols; ++j)
            std::cout << matrix[i * cols + j] << " ";
        std::cout << std::endl;
    }

    delete[] matrix; // освобождение одного блока

    return 0;
}
```

### 🔹 Объяснение

- Доступ к элементу: `matrix[i * cols + j]`
    
- Эффективнее по памяти (все элементы подряд)
    
- Простое выделение и освобождение
    

---

## 4️⃣ Отличия методов

|Метод|Преимущества|Недостатки|
|---|---|---|
|Массив указателей|Легко использовать привычный `matrix[i][j]`|Несколько выделений памяти, медленнее|
|Один блок|Быстрее, меньше накладных расходов|Доступ через `matrix[i * cols + j]`, нет привычной индексации|

---

## 5️⃣ Передача в функции

### Массив указателей:

```cpp
void printMatrix(int** m, int rows, int cols) {
    for(int i = 0; i < rows; ++i) {
        for(int j = 0; j < cols; ++j)
            std::cout << m[i][j] << " ";
        std::cout << std::endl;
    }
}
```

### Один большой блок:

```cpp
void printMatrix(int* m, int rows, int cols) {
    for(int i = 0; i < rows; ++i) {
        for(int j = 0; j < cols; ++j)
            std::cout << m[i*cols + j] << " ";
        std::cout << std::endl;
    }
}
```

---

## 6️⃣ Резюме для студента

- **Двумерный динамический массив**: массив массивов в куче, размеры задаются во время выполнения
    
- Доступ к элементам:
    
    - через `matrix[i][j]` (массив указателей)
        
    - через `matrix[i*cols + j]` (один блок)
        
- Память **обязательно освобождать** (`delete[]`)
    
- Используется, когда **размер массива заранее неизвестен**
    
