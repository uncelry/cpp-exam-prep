Разбираем тему подробно.

---

## 🔹 What is a memory leak when working with arrays? How to prevent it?

### 🔸 Перевод темы

**Что такое утечка памяти при работе с массивами и как её предотвратить**

---

## 1️⃣ Определение

**Утечка памяти (memory leak)** — это ситуация, когда **динамически выделенная память не освобождается**, а программа продолжает работать.

- Память остаётся **занятой в куче**, но на неё нет указателей
    
- Со временем, при больших объёмах данных или долгом выполнении программы, это может привести к **исчерпанию памяти** и сбоям
    

---

## 2️⃣ Пример утечки памяти с одномерным массивом

```cpp
#include <iostream>

int main() {
    int* arr = new int[5]; // выделили память

    arr[0] = 10; 
    arr[1] = 20;

    // забыли освободить память
    // delete[] arr; ❌

    return 0;
}
```

- Память под массив `arr` остаётся занятой после выхода из программы
    
- В маленьких программах это незаметно, но в больших — проблема
    

---

## 3️⃣ Пример с многомерным массивом

```cpp
int** matrix = new int*[3];
for(int i = 0; i < 3; ++i) {
    matrix[i] = new int[4];
}

// забыли delete[]
// delete[] matrix[i]; delete[] matrix; ❌
```

- Каждая строка выделялась динамически → нужно **освобождать каждую строку**, потом сам массив указателей
    
- Если это не сделать → утечка памяти
    

---

## 4️⃣ Как предотвратить утечку памяти

### 1️⃣ Всегда использовать `delete[]` после `new[]`

```cpp
delete[] arr;      // одномерный массив
delete[] matrix[i]; // каждая строка
delete[] matrix;    // массив указателей
```

---

### 2️⃣ Присваивать указателю `nullptr` после удаления

```cpp
delete[] arr;
arr = nullptr; // безопасно, больше не указывает на старую память
```

- Если случайно снова использовать `arr` — программа аварийно не упадёт
    

---

### 3️⃣ Использовать умные указатели (modern C++)

- `std::unique_ptr` или контейнеры `std::vector`, `std::array`
    
- Они автоматически освобождают память
    
- Пример с `vector`:
    

```cpp
#include <vector>
int main() {
    std::vector<int> arr(5, 0); // динамический массив на 5 элементов
    arr[0] = 10; // память управляется автоматически
}
```

- Никаких `new` и `delete` — утечек нет
    

---

### 4️⃣ Контроль при работе с многомерными массивами

- Если используете массив указателей, важно **в цикле удалить каждую строку**
    
- Потом удалить массив указателей
    

```cpp
for(int i = 0; i < rows; ++i)
    delete[] matrix[i];
delete[] matrix;
```

---

## 5️⃣ Итоги для студента

1. **Memory leak** — это динамическая память, которая не была освобождена
    
2. Чаще всего возникает при работе с **динамическими массивами** (`new[]`)
    
3. Чтобы предотвратить:
    
    - всегда использовать `delete[]`
        
    - присваивать указателю `nullptr` после удаления
        
    - предпочтительно использовать **умные указатели** и контейнеры STL (`vector`, `array`)
        
4. Для многомерных массивов освобождение должно происходить **в обратном порядке**
    
