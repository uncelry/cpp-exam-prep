Разбираем тему подробно.

---

## 🔹 How to resize a dynamic array

### 🔸 Перевод темы

**Как изменить размер динамического массива**

---

## 1️⃣ Основная идея

В C++ **обычный динамический массив**, созданный через `new[]`, **не может изменять свой размер** напрямую.

- Чтобы «изменить размер», нужно:
    
    1. Создать новый массив нужного размера
        
    2. Скопировать данные из старого массива
        
    3. Удалить старый массив (`delete[]`)
        
    4. Переназначить указатель на новый массив
        

> В современном C++ для изменения размера лучше использовать `std::vector`, который делает всё автоматически.

---

## 2️⃣ Пример изменения размера динамического массива вручную

```cpp
#include <iostream>
int main() {
    int n = 5;
    int* arr = new int[n]; // создаём массив из 5 элементов

    // инициализация
    for(int i = 0; i < n; ++i)
        arr[i] = i + 1;

    // хотим увеличить размер до 8
    int newSize = 8;
    int* newArr = new int[newSize]; // новый массив

    // копируем старые элементы
    for(int i = 0; i < n; ++i)
        newArr[i] = arr[i];

    // новые элементы инициализируем, например, нулями
    for(int i = n; i < newSize; ++i)
        newArr[i] = 0;

    delete[] arr; // освобождаем старый массив
    arr = newArr; // переназначаем указатель

    // проверка
    for(int i = 0; i < newSize; ++i)
        std::cout << arr[i] << " ";
    std::cout << std::endl;

    delete[] arr; // освобождаем память
    return 0;
}
```

Вывод:

```
1 2 3 4 5 0 0 0
```

---

## 3️⃣ Важные моменты

1. **Нельзя изменить размер старого массива** напрямую (`arr[n] = newSize;` ❌)
    
2. Всегда нужно **освобождать память старого массива**, чтобы не было **утечки**
    
3. Копирование данных может быть **дорогостоящим** для больших массивов → учитывайте при проектировании
    
4. Для уменьшения массива процесс аналогичный: создаём новый массив меньшего размера, копируем нужное количество элементов, удаляем старый
    

---

## 4️⃣ Лучший способ в современном C++

Использовать **`std::vector`**, который динамически меняет размер и управляет памятью автоматически:

```cpp
#include <vector>
#include <iostream>
int main() {
    std::vector<int> arr = {1,2,3,4,5};

    // увеличиваем размер до 8, новые элементы = 0
    arr.resize(8, 0);

    for(int x : arr)
        std::cout << x << " ";
    std::cout << std::endl;
}
```

- Не нужно вручную копировать и удалять память
    
- Размер можно менять в любое время
    
- Безопасно, утечек нет
    

---

## 5️⃣ Резюме для студента

- **Динамический массив через `new[]` нельзя напрямую изменить в размере**
    
- Для «изменения размера»:
    
    1. Создать новый массив нужного размера
        
    2. Скопировать старые элементы
        
    3. Освободить старый массив (`delete[]`)
        
    4. Переназначить указатель
        
- Для удобства и безопасности используйте **`std::vector`**, который управляет памятью автоматически
    
