**Оператор `if` в C++**

---

## 1️⃣ Перевод темы

**What is the if statement in C++? → Что такое оператор `if` в C++?**  
**Provide an example → Приведите пример**

---

## 2️⃣ Что такое `if`

**`if`** — это условный оператор, который позволяет:

- выполнять код **только при выполнении условия**
    
- выбирать между разными ветками выполнения программы
    

Проще:

> `if` отвечает на вопрос: **выполнить код или нет?**

---

## 3️⃣ Синтаксис оператора `if`

```cpp
if (condition) {
    // код, если condition == true
}
```

- `condition` — выражение, приводимое к `bool`
    
- если `condition == true`, блок выполняется
    

---

## 4️⃣ Простейший пример

```cpp
int x = 10;

if (x > 0) {
    std::cout << "x is positive\n";
}
```

---

## 5️⃣ `if` и логические выражения

```cpp
if (x > 0 && x < 100) {
    std::cout << "x is in range\n";
}
```

---

## 6️⃣ Оператор `else`

```cpp
if (x % 2 == 0) {
    std::cout << "even\n";
} else {
    std::cout << "odd\n";
}
```

---

## 7️⃣ Цепочка `if – else if – else`

```cpp
int score = 85;

if (score >= 90) {
    std::cout << "A\n";
} else if (score >= 75) {
    std::cout << "B\n";
} else {
    std::cout << "C\n";
}
```

---

## 8️⃣ `if` без фигурных скобок

```cpp
if (x > 0)
    std::cout << "positive\n";
```

⚠️ Опасно при добавлении строк:

```cpp
if (x > 0)
    std::cout << "positive\n";
    std::cout << "done\n"; // выполнится всегда!
```

📌 Рекомендуется **всегда использовать `{}`**.

---

## 9️⃣ `if` с инициализацией (C++17+)

```cpp
if (int x = getValue(); x > 0) {
    std::cout << x << '\n';
}
```

- `x` существует только внутри `if`
    
- удобно и безопасно
    

---

## 🔟 Что считается истинным условием

В `if`:

- `true` → выполняется
    
- `false` → не выполняется
    

Любое выражение приводится к `bool`:

```cpp
if (5)   // true
if (0)   // false
```

---

## 1️⃣1️⃣ Частые ошибки

❌ использовать = вместо ==  
❌ забывать `{}`  
❌ писать сложные условия без скобок  
❌ не понимать область видимости переменных

---

## 1️⃣2️⃣ Итог

Надо понимать:

✔ назначение оператора `if`  
✔ его синтаксис  
✔ использование `else` и `else if`  
✔ правила вычисления условий  
✔ область видимости в `if`
