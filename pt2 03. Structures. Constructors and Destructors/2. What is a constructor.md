Разберём тему **конструкторов в C++** чётко и структурировано.

---

# 1️⃣ Что такое конструктор?

**Конструктор** — это специальная функция-член класса, которая:

- имеет то же имя, что и класс
    
- не имеет возвращаемого типа
    
- вызывается автоматически при создании объекта
    
- инициализирует состояние объекта
    

Главная задача конструктора — привести объект в корректное начальное состояние.

---

# 2️⃣ Когда вызывается конструктор?

Конструктор вызывается автоматически:

- при создании локального объекта
    
    ```cpp
    A a;
    ```
    
- при создании объекта в динамической памяти
    
    ```cpp
    A* p = new A();
    ```
    
- при создании временного объекта
    
    ```cpp
    A();
    ```
    
- при передаче объекта по значению
    
- при возврате объекта из функции по значению
    

---

# 3️⃣ Типы конструкторов

---

# 🔹 1. Конструктор по умолчанию (Default Constructor)

### Определение:

Конструктор без параметров.

```cpp
class A {
public:
    A() {
        std::cout << "Default constructor\n";
    }
};
```

Вызывается:

```cpp
A obj;
```

📌 Если пользователь не объявил ни одного конструктора, компилятор может сгенерировать его автоматически.

---

# 🔹 2. Конструктор с параметрами

Используется для инициализации объекта значениями.

```cpp
class Point {
    int x, y;
public:
    Point(int a, int b) : x(a), y(b) {}
};
```

Использование:

```cpp
Point p(10, 20);
```

Позволяет создать объект сразу в корректном состоянии.

---

# 🔹 3. Конструктор копирования (Copy Constructor)

### Определение:

Создаёт новый объект как копию существующего.

### Сигнатура:

```cpp
ClassName(const ClassName& other);
```

Пример:

```cpp
class A {
public:
    A(const A& other) {
        std::cout << "Copy constructor\n";
    }
};
```

Вызывается при:

```cpp
A a1;
A a2 = a1;      // копирование
A a3(a1);       // тоже копирование
```

Также вызывается:

- при передаче по значению
    
- при возврате по значению
    

---

# 🔹 4. Конструктор перемещения (Move Constructor)

Появился в C++11.

Позволяет **переместить ресурсы** из временного объекта вместо копирования.

### Сигнатура:

```cpp
ClassName(ClassName&& other);
```

Пример:

```cpp
class A {
public:
    A(A&& other) {
        std::cout << "Move constructor\n";
    }
};
```

Вызывается при:

```cpp
A a1;
A a2 = std::move(a1);
```

Используется для:

- оптимизации работы с ресурсами
    
- ускорения контейнеров (`std::vector`, `std::string`)
    

---

# 🔹 5. Конструктор преобразования (Conversion Constructor)

Это конструктор с **одним параметром**, который позволяет создавать объект из значения другого типа.

```cpp
class Number {
    int value;
public:
    Number(int v) : value(v) {}
};
```

Теперь возможно:

```cpp
Number n = 10;  // int → Number
```

Это называется **неявным преобразованием**.

---

## Чтобы запретить неявное преобразование

Используется `explicit`:

```cpp
class Number {
public:
    explicit Number(int v) {}
};
```

Теперь:

```cpp
Number n = 10;  // ❌ ошибка
Number n(10);   // ✅
```

---

# 4️⃣ Порядок вызова при создании объекта

1. Конструкторы базовых классов
    
2. Инициализация полей (в порядке объявления)
    
3. Выполнение тела конструктора
    

---

# 5️⃣ Краткое сравнение

|Тип|Назначение|
|---|---|
|Default|Создание без параметров|
|С параметрами|Инициализация данными|
|Copy|Копирование объекта|
|Move|Перемещение ресурсов|
|Conversion|Преобразование из другого типа|

---

# Итог

Конструктор — это механизм:

- автоматической инициализации объекта
    
- управления ресурсами
    
- обеспечения корректного состояния
    

Понимание различий между копированием и перемещением особенно важно для современного C++.
