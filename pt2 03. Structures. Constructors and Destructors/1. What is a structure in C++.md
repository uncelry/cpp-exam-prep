Разберём тему **структур (struct) в C++**, их конструкторов/деструкторов и различия с классами.

---

# 1️⃣ Что такое структура (`struct`) в C++

В C++ `struct` — это **пользовательский тип данных**, который может содержать:

- поля (данные)
    
- методы
    
- конструкторы
    
- деструкторы
    
- операторы
    
- наследование
    
- модификаторы доступа
    

⚠ Важно: в C++ `struct` — это практически полноценный класс.

---

## Пример структуры

```cpp
struct Point {
    double x;
    double y;
};
```

---

# 2️⃣ Инициализация полей структуры

## 🔹 1. Агрегатная инициализация

Если структура — агрегат (без пользовательских конструкторов):

```cpp
Point p{1.0, 2.0};
```

или

```cpp
Point p = {1.0, 2.0};
```

---

## 🔹 2. Инициализация по умолчанию (C++11+)

Можно задать значения прямо в объявлении:

```cpp
struct Point {
    double x = 0.0;
    double y = 0.0;
};
```

---

## 🔹 3. Пользовательский конструктор

```cpp
struct Point {
    double x;
    double y;

    Point(double x_, double y_) : x(x_), y(y_) {}
};
```

---

# 3️⃣ Конструкторы и деструкторы в struct

Структуры могут иметь:

- конструктор по умолчанию
    
- параметризованный
    
- копирующий
    
- перемещающий
    
- деструктор
    

Пример:

```cpp
struct Resource {
    Resource() {
        std::cout << "Created\n";
    }

    ~Resource() {
        std::cout << "Destroyed\n";
    }
};
```

📌 Поведение полностью такое же, как у класса.

---

# 4️⃣ Формальное различие между struct и class

Единственное формальное различие в C++:

||struct|class|
|---|---|---|
|Доступ по умолчанию|public|private|
|Наследование по умолчанию|public|private|

---

## Пример

```cpp
struct S {
    int x;  // public
};

class C {
    int x;  // private
};
```

---

# 5️⃣ Практическое различие (по стилю использования)

Хотя технически они одинаковы, существует соглашение:

### 🔹 struct используют, когда:

- структура данных
    
- POD-тип
    
- простой контейнер данных
    
- DTO (data transfer object)
    

Пример:

```cpp
struct User {
    std::string name;
    int age;
};
```

---

### 🔹 class используют, когда:

- инкапсуляция
    
- логика поведения
    
- скрытые детали реализации
    
- сложная бизнес-логика
    

Пример:

```cpp
class BankAccount {
private:
    double balance;

public:
    void deposit(double amount);
};
```

---

# 6️⃣ Агрегат vs неагрегат

Структура является агрегатом если:

- нет пользовательских конструкторов
    
- нет приватных/защищённых членов
    
- нет виртуальных функций
    
- нет базовых классов
    

Агрегаты можно инициализировать списком.

---

# 7️⃣ Размер и размещение

Структуры:

- поля размещаются последовательно
    
- возможен padding из-за выравнивания
    

---

# 8️⃣ Пример полной структуры

```cpp
struct Person {
    std::string name;
    int age;

    Person(std::string n, int a)
        : name(n), age(a) {}

    ~Person() {
        std::cout << "Person destroyed\n";
    }

    void print() const {
        std::cout << name << " " << age << std::endl;
    }
};
```

---

# 9️⃣ Важный момент: C struct vs C++ struct

В C:

- только данные
    
- нет методов
    
- нет конструкторов
    

В C++:

- полноценный тип
    

---

# 🔟 Краткое резюме

### struct в C++:

- почти полностью эквивалентен class
    
- поддерживает конструкторы и деструкторы
    
- может иметь методы и наследование
    

### Единственное техническое отличие:

- struct → public по умолчанию
    
- class → private по умолчанию
    

### Практическое правило:

- struct — для данных
    
- class — для поведения и инкапсуляции
    

