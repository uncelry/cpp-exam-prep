Разберём тему **делегирующих конструкторов (delegating constructors)** — это механизм C++11 для переиспользования логики инициализации.

---

# 1️⃣ Что такое делегирующий конструктор?

**Делегирующий конструктор** — это конструктор, который **вызывает другой конструктор того же класса** для выполнения основной инициализации.

То есть один конструктор _делегирует_ работу другому.

---

# 2️⃣ Зачем это нужно?

Без делегирования часто возникает дублирование кода:

```cpp
class A {
    int x;
public:
    A() {
        x = 0;
    }

    A(int value) {
        x = value;
    }
};
```

Если логика сложная — код начинает дублироваться.

---

# 3️⃣ Принцип работы делегирования

В C++11 можно написать:

```cpp
class A {
    int x;
public:
    A(int value) : x(value) {}

    A() : A(0) {}  // делегирование
};
```

Здесь:

- `A()` вызывает `A(int)`
    
- инициализация выполняется **в целевом конструкторе**
    
- затем управление возвращается в тело вызывающего
    

---

# 4️⃣ Как работает цепочка вызовов (call chain)

Пример:

```cpp
class A {
    int x;
public:
    A(int value) : x(value) {
        std::cout << "Main constructor\n";
    }

    A() : A(10) {
        std::cout << "Delegating constructor\n";
    }
};
```

Создание:

```cpp
A obj;
```

Порядок выполнения:

1. Вызывается `A()`
    
2. Он делегирует `A(10)`
    
3. Выполняется `A(int)`
    
4. Возврат в тело `A()`
    
5. Выполняется его тело
    

Вывод:

```
Main constructor
Delegating constructor
```

---

# 5️⃣ Важные правила

## 🔹 1. Делегирование указывается в списке инициализации

```cpp
A() : A(0) {}
```

Нельзя:

```cpp
A() {
    A(0);  // ❌ создаёт временный объект!
}
```

---

## 🔹 2. Можно делегировать только одному конструктору

В списке инициализации может быть либо:

- делегирование
    
- либо инициализация полей
    

Но не оба одновременно.

❌ Нельзя:

```cpp
A() : A(5), x(0) {}  // ошибка
```

---

## 🔹 3. Нельзя создавать циклическое делегирование

```cpp
A() : A(5) {}
A(int x) : A() {}   // ❌ бесконечная рекурсия
```

Это приводит к ошибке компиляции.

---

# 6️⃣ Можно ли вызвать несколько конструкторов последовательно?

Прямо — нет.

В C++:

- для одного объекта выполняется **только один конечный (non-delegating) конструктор**
    
- можно создать цепочку делегирования
    
- но в итоге выполняется один основной конструктор
    

Пример цепочки:

```cpp
class A {
public:
    A(int x, int y) {
        std::cout << "Full constructor\n";
    }

    A(int x) : A(x, 0) {
        std::cout << "Second constructor\n";
    }

    A() : A(5) {
        std::cout << "Third constructor\n";
    }
};
```

Создание:

```cpp
A obj;
```

Порядок:

1. `A()`
    
2. → `A(int)`
    
3. → `A(int,int)`
    
4. выполняется тело `A(int,int)`
    
5. возврат в `A(int)`
    
6. возврат в `A()`
    

---

# 7️⃣ Полный корректный пример

```cpp
#include <iostream>

class Person {
    std::string name;
    int age;

public:
    Person(std::string n, int a)
        : name(n), age(a) {}

    Person(std::string n)
        : Person(n, 0) {}

    Person()
        : Person("Unknown") {}
};
```

Здесь:

- основной конструктор — самый полный
    
- остальные сокращённые версии делегируют ему
    

Это правильный стиль.

---

# 8️⃣ Преимущества делегирования

✔ Устранение дублирования  
✔ Централизация логики инициализации  
✔ Меньше ошибок  
✔ Улучшение поддержки кода

---

# 9️⃣ Краткое резюме

- Делегирующий конструктор вызывает другой конструктор того же класса
    
- Указывается в списке инициализации
    
- Можно построить цепочку вызовов
    
- В итоге выполняется один основной конструктор
    
- Нельзя делегировать нескольким конструкторам одновременно
    
- Нельзя создавать циклы
    
