Разбираем тему подробно.

---

## 🔹 How to create a one-dimensional dynamic array

### 🔸 Перевод темы

**Как создать одномерный динамический массив**

---

## 1️⃣ Определение

**Динамический массив** — это массив, **размер которого можно задать во время выполнения программы**.

- Память для него выделяется в **куче (heap)** с помощью оператора `new`
    
- После использования память нужно **освободить** с помощью `delete[]`
    
- Отличается от статического массива, который **фиксирован на этапе компиляции**
    

---

## 2️⃣ Создание одномерного динамического массива

```cpp
int n;
std::cout << "Enter size of array: ";
std::cin >> n;

// выделяем память для массива из n элементов
int* arr = new int[n];
```

- `arr` — указатель на первый элемент массива
    
- `n` — размер, заданный во время выполнения
    

---

## 3️⃣ Инициализация элементов

### 🔹 Через цикл

```cpp
for(int i = 0; i < n; ++i) {
    arr[i] = i * 10;
}
```

### 🔹 Используя `{}` (только при compile-time константе)

```cpp
int* arr = new int[5]{1, 2, 3, 4, 5}; // работает для фиксированного размера
```

---

## 4️⃣ Доступ к элементам

- С помощью индексов `[i]`
    
- С помощью указательной арифметики `*(arr + i)`
    

```cpp
std::cout << arr[2] << std::endl;   // доступ к 3-му элементу
std::cout << *(arr + 2) << std::endl; // то же самое
```

---

## 5️⃣ Освобождение памяти

После использования **обязательно**:

```cpp
delete[] arr; // освобождение динамической памяти
arr = nullptr; // безопасная практика
```

- Если не сделать `delete[]` → будет **утечка памяти (memory leak)**
    

---

## 6️⃣ Пример программы

```cpp
#include <iostream>
int main() {
    int n;
    std::cout << "Enter size of array: ";
    std::cin >> n;

    int* arr = new int[n]; // создание динамического массива

    // инициализация
    for(int i = 0; i < n; ++i) {
        arr[i] = i * 2;
    }

    // вывод
    for(int i = 0; i < n; ++i) {
        std::cout << arr[i] << " ";
    }
    std::cout << std::endl;

    delete[] arr; // освобождение памяти
    arr = nullptr;
}
```

---

## 7️⃣ Отличие от статического массива

|Критерий|Статический массив|Динамический массив|
|---|---|---|
|Размер|фиксированный|задаётся во время выполнения|
|Память|стек|куча (heap)|
|Освобождение|автоматически при выходе из области видимости|вручную `delete[]`|
|Можно менять размер|❌|❌ (нельзя напрямую; только создать новый массив)|
|Синтаксис|`int arr[5];`|`int* arr = new int[n];`|

---

## 8️⃣ Итоги для студента

- **Динамический массив**: размер задаётся во время выполнения, память в куче
    
- Обязательно использовать `delete[]` после окончания работы
    
- Доступ к элементам — как у обычного массива
    
- Используется, когда **размер заранее неизвестен**
    
