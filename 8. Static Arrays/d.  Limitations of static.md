Разбираем тему подробно.

---

## 🔹 Limitations of static arrays

### 🔸 Перевод темы

**Ограничения статических массивов**

---

## 1️⃣ Основное определение

**Статический массив** — это массив с **фиксированным размером**, известным на этапе компиляции.

Хотя статические массивы просты и удобны, у них есть **несколько ограничений**, которые важно понимать для экзамена.

---

## 2️⃣ Основные ограничения

### 1. Фиксированный размер

```cpp
int arr[5];
```

- Размер массива **не может быть изменён во время выполнения программы**
    
- Если нужно больше элементов — придётся создавать новый массив
    

---

### 2. Ограничение на стек

- Локальные статические массивы создаются **в стеке**
    
- Стек ограничен по размеру (обычно несколько мегабайт)
    
- Слишком большой массив вызовет **stack overflow**
    

```cpp
int largeArray[1000000]; // может упасть
```

---

### 3. Нет проверки выхода за границы

```cpp
int arr[5];
arr[10] = 42; // ❌ UB (undefined behavior)
```

- Компилятор **не проверяет индексы**
    
- Ошибки часто проявляются только во время выполнения
    

---

### 4. Массивы не знают свой размер

```cpp
void print(int arr[]) {
    std::cout << sizeof(arr); // вернёт размер указателя, а не массива
}
```

- Передавая массив в функцию, он деградирует в **указатель**
    
- Нужно отдельно передавать размер массива
    

---

### 5. Сложность работы с многомерными массивами

- При передаче в функцию нужно знать **количество столбцов**
    

```cpp
void print(int arr[][4], int rows);
```

- Если столбцов много и заранее неизвестно — неудобно
    
- Динамические массивы или векторы удобнее
    

---

### 6. Невозможность возвращать массив из функции

```cpp
int[5] getArray() { ... } // ❌ нельзя
```

- Массив **не может быть возвращаемым типом функции**
    
- Нужно использовать:
    
    - указатель / динамическую память
        
    - `std::array` или `std::vector`
        

---

### 7. Нет удобных методов

- Статический массив — просто блок памяти
    
- Нет методов для:
    
    - добавления элемента
        
    - удаления
        
    - изменения размера
        
- В современном C++ предпочтительны `std::array` и `std::vector`
    

---

## 3️⃣ Сводная таблица ограничений

|Ограничение|Пример|Последствие|
|---|---|---|
|Фиксированный размер|`int arr[10];`|Нельзя увеличить/уменьшить размер|
|Стековое размещение|`int arr[1000000];`|Stack overflow|
|Нет проверки индексов|`arr[10] = 5;`|Undefined behavior|
|Деградация в указатель при передаче в функцию|`void f(int arr[])`|sizeof(arr) = sizeof(pointer)|
|Не возвращается из функции|`int[5] getArray()`|Нужно использовать vector / array / указатель|
|Нет методов|-|Нет push_back, size, insert и т.д.|

---

## 4️⃣ Резюме для студента

- **Статический массив** прост и быстр, но:
    
    - Размер фиксирован
        
    - Нет защиты от ошибок с индексами
        
    - Ограничения стека для больших массивов
        
    - Неудобство при передаче и возврате из функций
        
- Для больших и динамических данных лучше использовать:
    
    - `std::vector` (динамический массив)
        
    - `std::array` (фиксированный размер, но безопаснее)
        
