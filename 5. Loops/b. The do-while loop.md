**Цикл `do-while` в C++**

---

## 1️⃣ Перевод темы

**The do-while loop → Цикл `do-while`**

---

## 2️⃣ Что такое цикл `do-while`

**`do-while`** — это цикл с постусловием, который:

- **сначала выполняет тело цикла**
    
- **потом проверяет условие**
    
- гарантирует **минимум одну итерацию**
    

Проще:

> «Сделай → потом проверь».

---

## 3️⃣ Синтаксис цикла `do-while`

```cpp
do {
    // тело цикла
} while (condition);
```

⚠️ Обрати внимание: **точка с запятой обязательна**.

---

## 4️⃣ Простейший пример

```cpp
int i = 0;

do {
    std::cout << i << " ";
    i++;
} while (i < 5);
```

Вывод:

```
0 1 2 3 4
```

---

## 5️⃣ Главное отличие от `while`

||while|do-while|
|---|---|---|
|Проверка условия|До цикла|После цикла|
|Минимум итераций|0|1|

---

## 6️⃣ Когда использовать `do-while`

✔ тело должно выполниться хотя бы один раз  
✔ ввод данных с повторным запросом  
✔ меню программ  
✔ подтверждение действий

---

## 7️⃣ Типичный пример: ввод пользователя

```cpp
int x;

do {
    std::cout << "Enter positive number: ";
    std::cin >> x;
} while (x <= 0);
```

---

## 8️⃣ Управление циклом

### 🔹 `break`

Выход из цикла:

```cpp
break;
```

### 🔹 `continue`

Переход к проверке условия:

```cpp
continue;
```

---

## 9️⃣ Область видимости

Переменные, объявленные:

- **вне цикла** — доступны в условии
    
- **внутри тела** — недоступны в условии
    

```cpp
do {
    int x = 5;
} while (x > 0); // ошибка
```

---

## 🔟 Ограничения `do-while`

❌ нельзя инициализировать переменную в условии  
❌ редко используется по сравнению с `for` и `while`  
❌ легко забыть `;`

---

## 1️⃣1️⃣ Частые ошибки

❌ забывают `;` после `while`  
❌ думают, что условие проверяется до цикла  
❌ используют `do-while`, когда цикл может не понадобиться  
❌ путают область видимости

---

## 1️⃣2️⃣ Итог

Надо должен понимать:

✔ что такое `do-while`  
✔ его синтаксис  
✔ отличие от `while`  
✔ гарантированное выполнение  
✔ типичные случаи использования
