**Цикл `for`. Эквивалентность `for` и `while`**

---

## 1️⃣ Перевод темы

**The for loop. Equivalence between for and while →  
Цикл `for`. Эквивалентность циклов `for` и `while`**

---

## 2️⃣ Что такое цикл `for`

**`for`** — это цикл, который удобно использовать, когда:

- известно количество итераций
    
- есть чёткая схема: _инициализация → условие → шаг_
    

---

## 3️⃣ Синтаксис цикла `for`

```cpp
for (initialization; condition; iteration) {
    // тело цикла
}
```

### Составные части:

1. **Initialization** — выполняется один раз перед началом цикла
    
2. **Condition** — проверяется перед каждой итерацией
    
3. **Iteration** — выполняется после каждой итерации
    

---

## 4️⃣ Простейший пример

```cpp
for (int i = 0; i < 5; ++i) {
    std::cout << i << " ";
}
```

Вывод:

```
0 1 2 3 4
```

---

## 5️⃣ Область видимости переменной цикла

```cpp
for (int i = 0; i < 3; ++i) {
    // i доступна здесь
}
// i недоступна здесь
```

`i` существует **только внутри цикла**.

---

## 6️⃣ Инициализация в `for` (C++17+)

```cpp
for (int i = 0, j = 10; i < j; ++i, --j) {
    // ...
}
```

Можно:

- объявлять несколько переменных
    
- использовать любые выражения
    

---

## 7️⃣ Эквивалентность `for` и `while`

Любой цикл `for` можно переписать как `while`.

### 🔹 `for`

```cpp
for (int i = 0; i < 5; ++i) {
    std::cout << i << " ";
}
```

### 🔹 Эквивалентный `while`

```cpp
int i = 0;
while (i < 5) {
    std::cout << i << " ";
    ++i;
}
```

---

## 8️⃣ Общая схема эквивалентности

```cpp
for (A; B; C) {
    D;
}
```

Эквивалентно:

```cpp
A;
while (B) {
    D;
    C;
}
```

---

## 9️⃣ Когда `for` лучше, чем `while`

✔ счётчики  
✔ перебор диапазона  
✔ массивы и контейнеры  
✔ более компактный и читаемый код

---

## 🔟 Когда `while` лучше, чем `for`

✔ неизвестно число итераций  
✔ ввод данных  
✔ ожидание события

---

## 1️⃣1️⃣ `break` и `continue` в `for`

```cpp
for (int i = 0; i < 10; ++i) {
    if (i == 5) break;
    if (i % 2 == 0) continue;
}
```

- `break` — выход из цикла
    
- `continue` — переход к шагу `iteration`
    

---

## 1️⃣2️⃣ Бесконечный цикл `for`

```cpp
for (;;) {
    // бесконечный цикл
}
```

Эквивалент:

```cpp
while (true) { }
```

---

## 1️⃣3️⃣ Частые ошибки

❌ забывают обновлять счётчик  
❌ неверное условие  
❌ путают `++i` и `i++` (логически)  
❌ выносят сложную логику в заголовок цикла

---

## 1️⃣4️⃣ Итог

Надо должен уметь:

✔ писать цикл `for`  
✔ понимать каждую его часть  
✔ переписывать `for` в `while`  
✔ выбирать подходящий тип цикла
