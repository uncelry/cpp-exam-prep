**Операторы управления: `break`, `continue` и безусловный `goto`**

---

## 1️⃣ Перевод темы

**Control statements: break, continue, and the unconditional goto (why it should not be used) →  
Операторы управления: `break`, `continue` и безусловный переход `goto` (почему его не стоит использовать)**

---

## 2️⃣ Что такое операторы управления

Операторы управления позволяют **изменять обычный поток выполнения программы**:

- преждевременно выходить из циклов
    
- пропускать части кода
    
- выполнять переходы в другое место программы
    

---

## 3️⃣ Оператор `break`

### 📌 Назначение

`break` **немедленно завершает выполнение ближайшего цикла или `switch`**.

---

### 🔹 Пример в цикле

```cpp
for (int i = 0; i < 10; ++i) {
    if (i == 5)
        break;
    std::cout << i << " ";
}
```

Вывод:

```
0 1 2 3 4
```

---

### 🔹 Пример в `switch`

```cpp
switch (x) {
case 1:
    std::cout << "One";
    break;
case 2:
    std::cout << "Two";
    break;
}
```

Без `break` произойдёт **проваливание (fallthrough)**.

---

## 4️⃣ Оператор `continue`

### 📌 Назначение

`continue` **прерывает текущую итерацию цикла** и:

- в `for` → переходит к шагу `iteration`
    
- в `while / do-while` → к проверке условия
    

---

### 🔹 Пример

```cpp
for (int i = 0; i < 5; ++i) {
    if (i == 2)
        continue;
    std::cout << i << " ";
}
```

Вывод:

```
0 1 3 4
```

---

## 5️⃣ Отличие `break` и `continue`

|Оператор|Что делает|
|---|---|
|`break`|Полностью завершает цикл|
|`continue`|Пропускает текущую итерацию|

---

## 6️⃣ Оператор `goto`

### 📌 Синтаксис

```cpp
goto label;

// ...
label:
    // код
```

---

### 🔹 Пример

```cpp
int i = 0;

start:
if (i < 5) {
    std::cout << i << " ";
    ++i;
    goto start;
}
```

Работает, но…

---

## 7️⃣ Почему `goto` **не рекомендуется использовать**

### ❌ 1. Нарушает структуру программы

Код становится **трудночитаемым и запутанным**  
→ эффект _“spaghetti code”_

---

### ❌ 2. Усложняет сопровождение

Трудно понять:

- откуда произошёл переход
    
- при каких условиях он выполнится
    

---

### ❌ 3. Опасен для инициализации объектов

Можно **перепрыгнуть через конструктор или деструктор**.

```cpp
goto skip;
std::string s = "Hello"; // пропущена инициализация
skip:
```

❌ **Ошибка компиляции**

---

### ❌ 4. Всегда есть альтернатива

`goto` можно заменить:

- циклами
    
- `break`
    
- `continue`
    
- `return`
    
- функциями
    

---

## 8️⃣ Когда `goto` всё же встречается

⚠️ Редкие случаи:

- выход из **глубоко вложенных циклов**
    
- низкоуровневый код
    
- legacy-код
    

Пример (иногда допустимый):

```cpp
for (...) {
    for (...) {
        if (error)
            goto end;
    }
}
end:
```

Но даже здесь лучше:

```cpp
bool done = false;
```

---

## 9️⃣ Экзаменационный акцент

Надо уметь объяснить:

✔ отличие `break` и `continue`  
✔ поведение в разных циклах  
✔ что такое `goto`  
✔ **почему его не стоит использовать**

---

## 🔟 Итог

- `break` — выход из цикла / `switch`
- `continue` — пропуск итерации
- `goto` — работает, но **плохая практика**
