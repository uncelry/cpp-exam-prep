**Цикл `while` в C++**

---

## 1️⃣ Перевод темы

**The while loop → Цикл `while`**

---

## 2️⃣ Что такое цикл `while`

**`while`** — это цикл с предусловием, который:

- **проверяет условие перед каждой итерацией**
    
- выполняет тело цикла, **пока условие истинно**
    

Проще:

> «Пока условие истинно — выполняй код».

---

## 3️⃣ Синтаксис цикла `while`

```cpp
while (condition) {
    // тело цикла
}
```

- `condition` — выражение, приводимое к `bool`
    
- если `condition == false` изначально — цикл не выполнится ни разу
    

---

## 4️⃣ Простейший пример

```cpp
int i = 0;

while (i < 5) {
    std::cout << i << " ";
    i++;
}
```

Вывод:

```
0 1 2 3 4
```

---

## 5️⃣ Цикл `while` и условие

```cpp
while (true) {
    // бесконечный цикл
}
```

Выход осуществляется через:

```cpp
break;
```

---

## 6️⃣ Использование `while`, когда число итераций неизвестно

`while` удобно использовать, когда:

- заранее **неизвестно**, сколько раз выполнится цикл
    
- выполнение зависит от внешнего условия
    

Пример:

```cpp
int x;
while (std::cin >> x) {
    // читаем числа, пока ввод корректен
}
```

---

## 7️⃣ Инициализация в условии `while` (C++17+)

❗ В отличие от `if` и `switch`, **`while` не поддерживает инициализацию в условии**.

```cpp
while (int x = getValue()) { } // ❌ ошибка
```

✔ Нужно писать отдельно:

```cpp
int x;
while ((x = getValue()) > 0) {
    // ...
}
```

---

## 8️⃣ `while` vs `do-while`

```cpp
do {
    // тело
} while (condition);
```

- `do-while` выполнится **хотя бы один раз**
    
- `while` может не выполниться ни разу
    

---

## 9️⃣ Управление циклом

### 🔹 `break`

Выход из цикла:

```cpp
break;
```

### 🔹 `continue`

Переход к следующей итерации:

```cpp
continue;
```

---

## 🔟 Частые ошибки

❌ забывают менять переменную условия → бесконечный цикл  
❌ используют = вместо ==  
❌ отсутствие `{}` при сложном теле  
❌ неправильная логика условия

---

## 1️⃣1️⃣ Когда использовать `while`

✔ количество итераций неизвестно  
✔ цикл зависит от ввода  
✔ чтение данных до конца  
✔ ожидание события

---

## 1️⃣2️⃣ Итог

Надо должен понимать:

✔ что такое `while`  
✔ синтаксис цикла  
✔ разницу с `do-while`  
✔ работу с условием  
✔ управление циклом
