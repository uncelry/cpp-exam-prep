**Побитовые операции в C++ (Bitwise operations)**

---

## 1️⃣ Перевод темы

**Bitwise operations in C++ → Побитовые операции в C++**

Операторы:

```
&   |   ^   ~   <<   >>
```

---

## 2️⃣ Что такое побитовые операции

**Побитовые операции** работают **не с числами как целыми значениями**, а **с их двоичным представлением** — бит за битом.

Пример:

```
5  = 00000101
3  = 00000011
```

---

## 3️⃣ Побитовое И (`&`)

### 🔹 Описание

Результат равен `1`, если **оба бита равны 1**.

```
1 & 1 → 1
1 & 0 → 0
0 & 1 → 0
0 & 0 → 0
```

### 🔹 Пример

```cpp
int a = 5;   // 0101
int b = 3;   // 0011

int c = a & b; // 0001 → 1
```

### 🔹 Применение

✔ проверка установленного бита  
✔ маски битов

```cpp
if (x & 1) { /* число нечётное */ }
```

---

## 4️⃣ Побитовое ИЛИ (`|`)

### 🔹 Описание

Результат `1`, если **хотя бы один бит равен 1**.

### 🔹 Пример

```cpp
int a = 5;   // 0101
int b = 3;   // 0011

int c = a | b; // 0111 → 7
```

### 🔹 Применение

✔ установка битов

```cpp
flags |= 0b0100;
```

---

## 5️⃣ Побитовое исключающее ИЛИ (`^`)

### 🔹 Описание

Результат `1`, если **биты различаются**.

```
1 ^ 1 → 0
1 ^ 0 → 1
```

### 🔹 Пример

```cpp
int a = 5;   // 0101
int b = 3;   // 0011

int c = a ^ b; // 0110 → 6
```

### 🔹 Применение

✔ переключение битов  
✔ обмен значений без временной переменной

```cpp
x ^= 1; // инвертировать младший бит
```

---

## 6️⃣ Побитовое НЕ (`~`)

### 🔹 Описание

Инвертирует каждый бит.

### 🔹 Пример

```cpp
unsigned char x = 5; // 00000101
unsigned char y = ~x; // 11111010
```

⚠️ Для signed типов важно учитывать **дополнительный код**.

---

## 7️⃣ Сдвиг влево (`<<`)

### 🔹 Описание

Сдвигает биты влево, справа дополняет нулями.

```cpp
int x = 3; // 00000011
x = x << 2; // 00001100 → 12
```

📌 Обычно эквивалентно умножению на `2^n`.

---

## 8️⃣ Сдвиг вправо (`>>`)

### 🔹 Описание

Сдвигает биты вправо.

```cpp
int x = 12; // 00001100
x = x >> 2; // 00000011 → 3
```

📌 Для `unsigned` — дополняется нулями  
📌 Для `signed` — **реализация-зависимо** (обычно сохраняется знак)

---

## 9️⃣ Побитовые операции и signed / unsigned

### 🔹 Пример

```cpp
signed char a = -1;   // 11111111
unsigned char b = ~a; // 00000000
```

👉 Использовать `unsigned` безопаснее для битовых операций.

---

## 🔟 Типичные применения побитовых операций

### ✔ Флаги и маски

```cpp
const int READ  = 1 << 0;
const int WRITE = 1 << 1;

int permissions = READ | WRITE;
```

### ✔ Проверка бита

```cpp
if (permissions & READ) { ... }
```

### ✔ Установка / сброс бита

```cpp
permissions |= WRITE;
permissions &= ~WRITE;
```

---

## 1️⃣1️⃣ Частые ошибки 

❌ путать `&` и `&&`  
❌ `|` и `||`  
❌ работать с отрицательными числами  
❌ забывать про приоритет операторов

---

## 1️⃣2️⃣ Итог 

Надо понимать:

✔ что побитовые операции работают с битами  
✔ назначение каждого оператора  
✔ разницу между логическими и побитовыми операциями  
✔ влияние signed / unsigned  
✔ практическое применение масок и флагов
