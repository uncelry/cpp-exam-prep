List and describe all fundamental data types in C++, including their size. Don’t forget fixed-width types. What does the data type affect?

## 1️⃣ Перевод темы

**Fundamental Data Types → Фундаментальные (базовые) типы данных в C++**

---

## 2️⃣ Что такое фундаментальные типы

Фундаментальные типы — это **основные встроенные типы**, предоставляемые языком C++ “из коробки”.  
Они не зависят от классов и библиотек и являются базой для всех других типов.

⚠️ Важно: **размеры многих типов зависят от компилятора и архитектуры** (обычно 32-бит или 64-бит система). Ниже дано:

- стандартное гарантированное минимальное требование C++
- и типичные размеры на современных 64-бит системах (GCC/Clang/MSVC).

---

## 3️⃣ Фундаментальные типы C++

### 🔹 Целочисленные типы

|Тип|Описание|Минимум по стандарту|Типично (64-bit)|
|---|---|---|---|
|`bool`|логический|1 байт (true/false)|1 байт|
|`char`|символьный байт|1 байт|1 байт|
|`signed char`|целый со знаком|≥1 байт|1 байт|
|`unsigned char`|целый без знака|≥1 байт|1 байт|
|`short` / `short int`|короткое целое|≥2 байта|2 байта|
|`unsigned short`|без знака|≥2 байта|2 байта|
|`int`|стандартное целое|≥2 байта|4 байта|
|`unsigned int`|без знака|≥2 байта|4 байта|
|`long`|“длинное” целое|≥4 байта|4 или 8 байт|
|`unsigned long`|без знака|≥4 байта|4 или 8 байт|
|`long long`|очень длинное целое|≥8 байт|8 байт|
|`unsigned long long`|без знака|≥8 байт|8 байт|

---

### 🔹 Символьные типы Unicode

|Тип|Размер|Назначение|
|---|---|---|
|`char16_t`|2 байта|UTF-16 кодовый элемент|
|`char32_t`|4 байта|UTF-32 кодовый элемент|
|`wchar_t`|зависит от платформы|широкие символы (старый механизм)|

---

### 🔹 Числа с плавающей точкой

|Тип|Типично|Назначение|
|---|---|---|
|`float`|4 байта|одинарная точность|
|`double`|8 байт|двойная точность|
|`long double`|8, 12 или 16 байт|расширенная точность (зависит от компилятора)|

---

### 🔹 Специальные типы

|Тип|Назначение|
|---|---|
|`void`|отсутствие значения|
|`std::nullptr_t`|тип для `nullptr`|

---

## 4️⃣ Фиксированные типы (fixed-width) — **ОЧЕНЬ ВАЖНО**

C++ предоставляет типы из заголовка

`#include <cstdint>`

которые **гарантируют точный размер в байтах** на любой платформе.

### 🔹 Фиксированные целочисленные типы

|Тип|Размер|
|---|---|
|`int8_t` / `uint8_t`|1 байт|
|`int16_t` / `uint16_t`|2 байта|
|`int32_t` / `uint32_t`|4 байта|
|`int64_t` / `uint64_t`|8 байт|

Есть также:

- `intptr_t`, `uintptr_t` — типы, способные хранить адрес указателя
- `int_fastN_t` — “наиболее быстрый минимум N бит”
- `int_leastN_t` — “наименьший тип, не меньше N бит”

---

## 5️⃣ Диапазоны значений (коротко)

(Типично на 64-бит системах)

- `int`: от −2 147 483 648 до 2 147 483 647
- `unsigned int`: 0 … 4 294 967 295
- `long long`: −9e18 … 9e18
- `uint64_t`: 0 … 18e18
- `float`: ≈ 7 знаков точности
- `double`: ≈ 15 знаков точности

---

## 6️⃣ На что влияет выбор типа?

Тип данных влияет на:

1️⃣ **Размер в памяти**

- `int` vs `long long`
- `float` vs `double`

2️⃣ **Диапазон значений**

- нельзя хранить большие числа в маленьком типе

3️⃣ **Точность**

- особенно важно для `float` vs `double`

4️⃣ **Скорость выполнения**

- меньшие типы → иногда быстрее
- `int_fast32_t` оптимизирован под платформу

5️⃣ **Представление данных**

- целые → точные
- `float/double` → возможны ошибки округления

6️⃣ **Совместимость и стандарт**

- ABI / взаимодействие с библиотеками
- сетевые протоколы → всегда fixed-width типы

7️⃣ **Перегрузку операторов и функции**  
Функция может вести себя по-разному для разных типов.

---

## 7️⃣ Итог

Надо понимать:

- тип влияет на память, диапазон, точность и производительность
- стандартные размеры могут отличаться на разных платформах
- **fixed-width типы (`int32_t`, `uint64_t`) — лучший выбор**, когда нужен гарантированный размер
- `double` чаще предпочтительнее `float`
- `long double` нужен только для научных вычислений

