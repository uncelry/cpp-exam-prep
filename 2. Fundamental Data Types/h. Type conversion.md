**Преобразование типов (Type conversion) в C++**

---

## 1️⃣ Перевод темы

**Type conversion → Преобразование типов**

---

## 2️⃣ Что такое преобразование типов

**Преобразование типов** — это процесс изменения значения одного типа данных в значение другого типа.

Пример:

```cpp
int a = 5;
double b = a; // int → double
```

---

## 3️⃣ Виды преобразований типов в C++

В C++ различают **неявные** и **явные** преобразования.

---

## 4️⃣ Неявное преобразование типов (Implicit conversion)

Происходит автоматически, без участия программиста.

### 🔹 Пример

```cpp
int a = 10;
double b = a; // автоматически → 10.0
```

### 🔹 Арифметические преобразования

Если в выражении участвуют разные типы:

```cpp
int a = 5;
double b = 2.5;

double c = a + b; // a → double
```

Это называется **обычные арифметические преобразования**.

---

## 5️⃣ Целочисленные продвижения (Integer promotions)

Малые целочисленные типы (`char`, `bool`, `short`) автоматически приводятся к `int` перед вычислениями.

```cpp
char c = 10;
int x = c + 5; // char → int
```

---

## 6️⃣ Опасности неявных преобразований

### 🔹 Потеря данных

```cpp
double x = 3.9;
int y = x; // y = 3
```

### 🔹 Signed / unsigned

```cpp
int a = -1;
unsigned int b = 1;

if (a < b) { ... } // неожиданное поведение
```

---

## 7️⃣ Явное преобразование типов (Explicit conversion)

Когда программист явно указывает, как преобразовать тип.

---

### 🔹 C-style cast (НЕ рекомендуется)

```cpp
int a = (int)3.7;
```

Опасен, потому что:

- неясно, что именно происходит
    
- может скрывать ошибки
    

---

### 🔹 C++-style casts (рекомендуется)

#### `static_cast`

Обычные, безопасные преобразования:

```cpp
double x = 3.14;
int y = static_cast<int>(x);
```

---

#### `const_cast`

Убирает или добавляет `const`:

```cpp
const int a = 10;
int& b = const_cast<int&>(a);
```

⚠️ Опасно, если объект изначально был `const`.

---

#### `reinterpret_cast`

Низкоуровневое преобразование битов:

```cpp
int x = 42;
char* p = reinterpret_cast<char*>(&x);
```

Используется редко, в системном коде. Может менять тип указателей/ссылок

---

#### `dynamic_cast`

Используется с полиморфизмом (наследование):

```cpp
Base* b = new Derived;
Derived* d = dynamic_cast<Derived*>(b);
```

Требует `virtual`.

---

## 8️⃣ Пользовательские преобразования

Можно определить свои преобразования через:

- конструкторы
    
- операторы приведения
    

Пример:

```cpp
class A {
public:
    A(int x) {}
};

A obj = 5; // int → A
```

---

## 9️⃣ Неявные vs явные — что лучше?

✔ неявные — удобны, но опасны  
✔ явные — безопаснее и читаемее

Современный C++ предпочитает:

```cpp
static_cast<T>(value)
```

---

## 🔟 Частые ошибки

❌ забывают про потерю данных  
❌ путают signed и unsigned  
❌ используют C-style cast  
❌ не понимают, в каком типе происходит вычисление

---

## 1️⃣1️⃣ Итог

Надо понимать:

✔ что такое преобразование типов  
✔ разницу между неявным и явным  
✔ целочисленные продвижения  
✔ опасности потери данных  
✔ разницу между `static_cast`, `reinterpret_cast`, `const_cast`, `dynamic_cast`  
✔ почему C-style cast — плохо

