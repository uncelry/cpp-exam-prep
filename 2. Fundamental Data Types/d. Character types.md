Character types (char, char8_t, char16_t, char32_t). Arithmetic with character types. ASCII table — what it is and why it’s used.

## 🔤 Символьные типы в C++

В C++ символы — это не “буквы”, а числа, которые кодируют символы по определённым таблицам кодировок.

### 1️⃣ Основные символьные типы:

### `char`

- Исторический базовый тип для символов
- Обычно 1 байт (8 бит), но стандарт гарантирует только **не меньше 8 бит**
- Может быть:
    - `signed char` — может хранить отрицательные значения
    - `unsigned char` — только положительные
    - просто `char` — ведёт себя как signed или unsigned в зависимости от компилятора
- Используется для хранения символов ASCII или байтов необработанных данных

Пример:

`char c = 'A';`

---

### `char8_t` (C++20)

- Тип для символов в кодировке **UTF-8**
- Размер: 1 байт (8 бит)
- **Отличие от char** — это отдельный тип, чтобы компилятор воспринимал его как текст в UTF-8, а не просто байты
- Нельзя напрямую присвоить `char` → `char8_t` без явного преобразования

Пример:

`char8_t c = u8'A';`

---

### `char16_t`

- Тип для символов кодировки **UTF-16**
- Размер: 2 байта (16 бит)
- Поддерживает большую часть символов мира (европейские, кириллица, азиатские и т.д.)
- Часто используется в Windows API

Пример:

`char16_t c = u'A';`

---

### `char32_t`

- Тип для символов кодировки **UTF-32**
- Размер: 4 байта (32 бита)
- Может хранить **любой символ Unicode** в одном значении

Пример:

`char32_t c = U'Ж';`

---

## ➕ Арифметика с символьными типами

Хотя это «символы», компилятор хранит их как числа, поэтому с ними можно выполнять арифметику.

Пример:

```
char c = 'A'; 
c = c + 1;   // теперь 'B'
```

Почему это работает?

- `'A'` в ASCII равен 65
- `'B'` = 66
- Мы просто увеличили число — символ поменялся

Можно делать:

- сравнение
- увеличение/уменьшение
- разность символов

Пример:

```
char c = 'a'; 
char d = 'z'; 
int diff = d - c; // 25
```

⚠️ Важно: арифметика с Unicode сложнее, т.к. символы занимают 2–4 байта и не всегда идут подряд логически (например, эмодзи).

---

## 🧾 Таблица ASCII — что это и зачем

**ASCII (American Standard Code for Information Interchange)** — это историческая таблица символов.

Размер: 128 символов (коды 0–127)

Содержит:

- Управляющие символы (0–31) — переход строки, сигнал, табуляция и т.п.
- Цифры '0'–'9'
- Английские буквы 'A'–'Z', 'a'–'z'
- Знаки пунктуации
- Пробел

Пример:

```
'A' = 65 
'a' = 97 
'0' = 48 
' ' (space) = 32
```

### Зачем нужна?

- Базовая кодировка для `char`
- Совместимость со старыми системами
- Удобна для арифметики
- Основа для Unicode

---

## ⚠️ Почему ASCII недостаточно

ASCII не поддерживает:

- кириллицу
- китайский
- японский
- эмодзи
- другие языки

Поэтому появился Unicode → UTF-8 / UTF-16 / UTF-32

---

## 📝 Итог

1️⃣ Символ в C++ — это **число**, которое кодирует букву  
2️⃣ Есть разные символьные типы:

- `char` — старый, маленький, чаще для ASCII и байтов
- `char8_t` — UTF-8
- `char16_t` — UTF-16
- `char32_t` — UTF-32

3️⃣ С символами можно выполнять арифметику, потому что за ними стоят числа  
4️⃣ ASCII — старая таблица кодов символов (основа текстовых систем)  
5️⃣ Для современных текстов нужен Unicode и расширенные символьные типы

