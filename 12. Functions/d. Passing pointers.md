Разбираем тему подробно, в формате для подготовки к экзамену.

---

## 🔹 Passing pointers, arrays, references, constant references and pointers. Peculiarities

### 🔸 Перевод темы

**Передача указателей, массивов, ссылок, константных ссылок и указателей. Особенности**

---

## 1️⃣ Передача указателя в функцию

### 🔹 Синтаксис

```cpp
void foo(int* p) {
    *p = 10;
}

int main() {
    int x = 5;
    foo(&x);
}
```

### 🔹 Особенности

- В функцию передаётся **копия указателя**
    
- Можно изменять объект, на который он указывает
    
- Можно передать `nullptr`
    

---

### 🔹 Константность указателя

```cpp
void f1(const int* p); // нельзя менять *p
void f2(int* const p); // нельзя менять p
```

---

## 2️⃣ Передача массива в функцию

### 🔹 Что происходит на самом деле

```cpp
void print(int arr[], int size);
```

Фактически это:

```cpp
void print(int* arr, int size);
```

➡️ Массив **деградирует в указатель**  
➡️ Размер массива **теряется**

---

### 🔹 Почему нужен size

```cpp
void print(const int* arr, int size) {
    for (int i = 0; i < size; ++i)
        std::cout << arr[i];
}
```

---

## 3️⃣ Передача ссылки

### 🔹 Обычная ссылка

```cpp
void foo(int& x) {
    x = 10;
}
```

- Нет копии объекта
    
- Нельзя передать `nullptr`
    
- Всегда привязана к объекту
    

---

## 4️⃣ Константная ссылка (`const T&`)

### 🔹 Самый важный вариант

```cpp
void print(const std::string& s);
```

Особенности:

- Нет копирования
    
- Объект нельзя изменить
    
- Можно передать:
    
    - временные объекты
        
    - литералы
        
    - r-value
        

```cpp
print("Hello");
print(std::string("Hi"));
```

---

## 5️⃣ Сравнение способов передачи

|Способ|Можно менять объект|Можно передать nullptr|Копирование|
|---|---|---|---|
|По значению|❌|❌|✅|
|Указатель|✅|✅|копия указателя|
|Ссылка|✅|❌|❌|
|const ссылка|❌|❌|❌|

---

## 6️⃣ Указатели и массивы — нюансы

```cpp
void f(int* p);
void f(int arr[]);
```

➡️ **Одинаковые сигнатуры**

---

## 7️⃣ Передача многомерных массивов

```cpp
void f(int arr[][3], int rows);
```

- Размер второго измерения обязателен
    
- Или использовать указатели / `std::vector`
    

---

## 8️⃣ Типичные ошибки

1. Забыли передать размер массива
    
2. Возвращают ссылку на локальный объект
    
3. Не используют `const`, где можно
    
4. Ожидают, что массив передаётся целиком
    

---

## 9️⃣ Когда что использовать

- **По значению** — для простых типов
    
- **const T&** — для чтения сложных объектов
    
- **T&** — если нужно изменить объект
    
- **T*** — если возможен `nullptr` или динамическая память
    
- **Массивы** — всегда с размером
    

---

## 🔟 Кратко для экзамена

- Массив → указатель при передаче
    
- Размер массива теряется
    
- Ссылка всегда указывает на объект
    
- Указатель может быть `nullptr`
    
- `const` — ключ к безопасному интерфейсу
    
