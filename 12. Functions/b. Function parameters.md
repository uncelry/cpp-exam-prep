Разбираем тему подробно, в формате для подготовки к экзамену.

---

## 🔹 Function parameters. Copying of parameters. Default parameters

### 🔸 Перевод темы

**Параметры функций. Копирование параметров. Параметры по умолчанию**

---

## 1️⃣ Параметры функции — базовое понятие

**Параметры функции** — это переменные, которые:

- объявляются в сигнатуре функции
    
- получают значения при вызове функции
    
- существуют **только внутри функции**
    

Пример:

```cpp
int add(int a, int b) {
    return a + b;
}
```

Здесь `a` и `b` — параметры функции.

---

## 2️⃣ Копирование параметров (передача по значению)

### 🔹 Передача по значению (pass by value)

По умолчанию параметры **копируются**:

```cpp
void foo(int x) {
    x = 10;
}

int main() {
    int a = 5;
    foo(a);
    // a == 5
}
```

Что происходит:

- создаётся **копия** `a`
    
- изменения внутри функции **не влияют** на исходную переменную
    

---

### 🔹 Стоимость копирования

Для простых типов (`int`, `double`) — дёшево.  
Для сложных типов (`std::string`, `std::vector`) — **дорого**.

```cpp
void print(std::string s); // копия строки
```

---

## 3️⃣ Альтернатива копированию

### 🔹 Передача по ссылке

```cpp
void foo(int& x) {
    x = 10;
}
```

Изменяет оригинальный объект.

---

### 🔹 Передача по `const` ссылке (рекомендуется)

```cpp
void print(const std::string& s);
```

- нет копирования
    
- нельзя изменить объект
    
- самый частый вариант на практике
    

---

## 4️⃣ Параметры по умолчанию (default parameters)

### 🔹 Определение

**Параметр по умолчанию** — это параметр, которому задано значение, используемое если аргумент не передан.

```cpp
void log(int level = 1);
```

---

### 🔹 Пример использования

```cpp
void print(int x, int y = 0) {
    std::cout << x << " " << y << std::endl;
}

print(5);     // x=5, y=0
print(5, 10); // x=5, y=10
```

---

## 5️⃣ Правила параметров по умолчанию

### 🔹 Только справа

❌ Ошибка:

```cpp
void foo(int a = 1, int b);
```

✅ Правильно:

```cpp
void foo(int a, int b = 1);
```

---

### 🔹 Задаются один раз

Обычно — **в объявлении** (в `.h`):

```cpp
// header.h
void foo(int x = 10);
```

```cpp
// source.cpp
void foo(int x) { }
```

❌ Нельзя повторять значение по умолчанию.

---

## 6️⃣ Default parameters и ODR

- Значения по умолчанию **подставляются на этапе компиляции**
    
- Все TU должны видеть **одинаковые значения**
    
- Несоответствие → логические ошибки
    

---

## 7️⃣ Параметры и перегрузка функций

```cpp
void foo(int x);
void foo(int x, int y = 0);
```

❌ Двусмысленность:

```cpp
foo(5); // какая функция?
```

---

## 8️⃣ Порядок вычисления аргументов

(важно упомянуть на экзамене)

- В C++ порядок вычисления аргументов **не определён** (до C++17)
    
- Не стоит полагаться на порядок
    

```cpp
foo(i++, i++);
```

---

## 9️⃣ Частые ошибки

1. Передача больших объектов по значению
    
2. Забыли `const` у ссылки
    
3. Параметры по умолчанию в `.cpp`
    
4. Конфликт с перегрузкой
    
5. Ожидание, что default-параметры «живут» в рантайме
    

---

## 🔟 Кратко для экзамена

- По умолчанию параметры **копируются**
    
- Копирование безопасно, но может быть дорогим
    
- Для чтения — `const T&`
    
- Параметры по умолчанию задаются **справа**
    
- Значения подставляются **на этапе компиляции**
    
- Default-параметры не участвуют в перегрузке
    
