**Динамическое выделение памяти. Операторы `new` и `delete`. Утечки памяти**

---

## 1️⃣ Перевод темы

**Dynamic memory allocation. new and delete operators. Memory leaks →  
Динамическое выделение памяти. Операторы `new` и `delete`. Утечки памяти**

---

## 2️⃣ Что такое динамическая память

**Динамическая память** — это память, которая:

- выделяется **во время выполнения программы**
    
- существует **пока мы её явно не освободим**
    
- размещается в области памяти, называемой **кучей (heap)**
    

В отличие от:

- автоматической памяти (stack)
    
- статической памяти
    

---

## 3️⃣ Зачем нужно динамическое выделение памяти

Динамическая память используется, когда:

- размер данных неизвестен на этапе компиляции
    
- объект должен жить дольше области видимости
    
- требуется гибкое управление временем жизни
    

---

## 4️⃣ Оператор `new`

### 📌 Назначение

`new`:

- выделяет память в куче
    
- вызывает конструктор объекта
    
- возвращает указатель
    

---

### 🔹 Пример

```cpp
int* p = new int(10);
```

Эквивалентно:

- выделить память под `int`
    
- записать `10`
    
- вернуть `int*`
    

---

## 5️⃣ Оператор `delete`

### 📌 Назначение

`delete`:

- вызывает деструктор
    
- освобождает память, выделенную через `new`
    

---

### 🔹 Пример

```cpp
delete p;
p = nullptr;
```

---

## 6️⃣ Динамические массивы: `new[]` и `delete[]`

```cpp
int* arr = new int[5];
delete[] arr;
```

❗ **Очень важно**:

- `new` ↔ `delete`
    
- `new[]` ↔ `delete[]`
    

---

## 7️⃣ Что будет при несоответствии

```cpp
int* arr = new int[5];
delete arr; // ❌ UB
```

---

## 8️⃣ Пример с объектами

```cpp
std::string* s = new std::string("Hello");
delete s;
```

- конструктор вызывается в `new`
    
- деструктор — в `delete`
    

---

## 9️⃣ Что такое утечка памяти (memory leak)

**Утечка памяти** — это ситуация, когда:

- память была выделена
    
- но ссылка на неё потеряна
    
- и `delete` никогда не вызывается
    

---

## 🔟 Пример утечки памяти

```cpp
void f() {
    int* p = new int(10);
} // ❌ p потерян, delete не вызван
```

---

## 1️⃣1️⃣ Последствия утечек памяти

- рост потребления памяти
    
- замедление программы
    
- падение приложения
    
- критично для серверов и встроенных систем
    

---

## 1️⃣2️⃣ Основные причины утечек

- забыли `delete`
    
- преждевременный `return`
    
- исключения
    
- перезапись указателя
    

```cpp
p = new int(5);
p = new int(10); // ❌ первая область потеряна
```

---

## 1️⃣3️⃣ Как избежать утечек

### ✅ 1. Используй `delete` всегда

```cpp
delete p;
p = nullptr;
```

---

### ✅ 2. Используй RAII

```cpp
std::unique_ptr<int> p = std::make_unique<int>(5);
```

---

### ✅ 3. Избегай `new` в современном C++

✔ контейнеры STL  
✔ умные указатели

---

### ✅ 4. Обрабатывай исключения

```cpp
try {
    int* p = new int[100];
    // ...
    delete[] p;
} catch (...) {
    // утечка
}
```

---

## 1️⃣4️⃣ Инструменты для поиска утечек

- Valgrind
    
- AddressSanitizer
    
- LeakSanitizer
    

---

## 1️⃣5️⃣ Частые ошибки студентов

❌ путают `delete` и `delete[]`  
❌ думают, что `delete` обнуляет указатель  
❌ забывают освобождать память  
❌ используют `new` без необходимости

---

## 1️⃣6️⃣ Экзаменационный минимум

Студент должен уметь:

✔ объяснить, зачем нужна динамическая память  
✔ правильно использовать `new/delete`  
✔ объяснить, что такое утечка памяти  
✔ назвать способы её предотвращения

---

## 1️⃣7️⃣ Итог

- `new` / `delete` — ручное управление памятью
    
- ошибки → UB и утечки
    
- в современном C++ **лучше избегать `new`**
    
