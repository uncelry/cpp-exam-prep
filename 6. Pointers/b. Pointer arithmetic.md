**Арифметика указателей в C++**

---

## 1️⃣ Перевод темы

**Pointer arithmetic → Арифметика указателей**

---

## 2️⃣ Что такое арифметика указателей

**Арифметика указателей** — это операции, позволяющие:

- перемещать указатель по памяти
    
- работать с массивами через указатели
    

Важно:

> арифметика указателей работает **в единицах размера типа**, а не в байтах.

---

## 3️⃣ Базовая идея

```cpp
int arr[3] = {10, 20, 30};
int* p = arr;   // указывает на arr[0]
```

```
p     → arr[0]
p + 1 → arr[1]
p + 2 → arr[2]
```

---

## 4️⃣ Операции, разрешённые для указателей

### ✔ Допустимые операции

- `p + n`
    
- `p - n`
    
- `p++`, `p--`
    
- `p1 - p2` (если указывают в один массив)
    
- сравнение (`==`, `!=`, `<`, `>` — ограниченно)
    

---

### ❌ Недопустимые операции

- `p + p`
    
- `p * 2`
    
- арифметика между указателями разных массивов
    

---

## 5️⃣ Пример: перемещение по массиву

```cpp
int arr[] = {1, 2, 3, 4};
int* p = arr;

std::cout << *p << "\n";     // 1
std::cout << *(p + 1) << "\n"; // 2
std::cout << *(p + 3) << "\n"; // 4
```

---

## 6️⃣ Почему `p + 1` ≠ адрес + 1 байт

```cpp
int x;
int* p = &x;

p + 1 == (char*)p + sizeof(int)
```

Если `sizeof(int) == 4`, то:

- `p + 1` → адрес + 4 байта
    

---

## 7️⃣ Инкремент и декремент

```cpp
int arr[] = {10, 20, 30};
int* p = arr;

std::cout << *p << "\n"; // 10
p++;
std::cout << *p << "\n"; // 20
```

---

## 8️⃣ Разность указателей

```cpp
int arr[] = {1, 2, 3, 4};
int* p1 = &arr[1];
int* p2 = &arr[4];

std::ptrdiff_t diff = p2 - p1; // 3
```

- результат — **количество элементов**
    
- тип результата: `std::ptrdiff_t`
    

---

## 9️⃣ Указатели и массивы

```cpp
arr[i] == *(arr + i)
```

Это фундаментальное правило C++.

---

## 🔟 Ограничения и правила безопасности

### ⚠️ Выход за границы массива

```cpp
p = arr + 4;   // допустимо (one-past-the-end)
*p;            // ❌ UB
```

✔ Разрешено указывать **на элемент после последнего**  
❌ Запрещено разыменовывать его

---

## 1️⃣1️⃣ Сравнение указателей

```cpp
if (p1 < p2) { }
```

Допустимо **только если они указывают в один массив**.

---

## 1️⃣2️⃣ Частые ошибки

❌ считают, что `p + 1` = +1 байт  
❌ выход за границы массива  
❌ разыменование `one-past-the-end`  
❌ арифметика между разными массивами

---

## 1️⃣3️⃣ Экзаменационный акцент

Надо уметь объяснить:

✔ как работает `p + n`  
✔ почему размер типа важен  
✔ разницу между адресами и элементами  
✔ что такое `one-past-the-end`

---

## 1️⃣4️⃣ Итог

- Арифметика указателей — основа работы с массивами
    
- Все операции считаются **в элементах**
    
- Строго ограничена стандартом C++
    
