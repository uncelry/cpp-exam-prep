**Указатель на `void` (void pointer) в C++**

---

## 1️⃣ Перевод темы

**Void pointer → Указатель на `void`**

---

## 2️⃣ Что такое `void*`

**`void*`** — это указатель, который может **хранить адрес объекта любого типа**,  
но **не знает**, на объект какого типа он указывает.

Проще:

> `void*` — «типонезависимый» указатель.

---

## 3️⃣ Объявление и инициализация

```cpp
int x = 10;
double d = 3.14;

void* p1 = &x;
void* p2 = &d;
```

✔ допустимо  
❌ но тип данных потерян

---

## 4️⃣ Почему `void*` нельзя разыменовывать

```cpp
void* p = &x;
*p; // ❌ ошибка компиляции
```

Причина:

- компилятор **не знает размер и тип объекта**
    
- не может корректно выполнить разыменование
    

---

## 5️⃣ Приведение типа (casting)

Перед разыменованием `void*` **обязательно** привести к конкретному типу:

```cpp
void* p = &x;
int* ip = static_cast<int*>(p);

std::cout << *ip; // 10
```

---

## 6️⃣ Арифметика с `void*`

❌ **Запрещена в C++**

```cpp
void* p;
p++; // ❌ ошибка
```

Почему:

- неизвестен размер шага
    

(В C разрешено как расширение, в C++ — нет)

---

## 7️⃣ `void*` и `const`

```cpp
const int x = 10;
const void* p = &x;
```

- `const void*` → нельзя менять объект
    
- но можно привести обратно к `const int*`
    

---

## 8️⃣ Где используется `void*`

### ✔ Исторически

- C API
    
- низкоуровневые библиотеки
    
- функции с универсальными данными
    

---

### ✔ Пример: `qsort` (из C)

```cpp
void qsort(
    void* base,
    size_t num,
    size_t size,
    int (*cmp)(const void*, const void*)
);
```

---

## 9️⃣ Почему `void*` **не любят** в современном C++

❌ нет проверки типов  
❌ небезопасные приведения  
❌ ошибки во время выполнения  
❌ хуже читаемость

---

## 🔟 Чем заменяют `void*` в C++

✔ шаблоны  
✔ перегрузка функций  
✔ `std::variant`  
✔ `std::any`  
✔ умные указатели

---

## 1️⃣1️⃣ Сравнение: `void*` vs шаблоны

|Критерий|`void*`|Шаблоны|
|---|---|---|
|Безопасность типов|❌|✔|
|Проверка на этапе компиляции|❌|✔|
|Читаемость|❌|✔|

---

## 1️⃣2️⃣ Частые ошибки

❌ разыменовывают `void*`  
❌ забывают приведение типа  
❌ делают арифметику  
❌ теряют `const`

---

## 1️⃣3️⃣ Экзаменационный минимум

✔ объяснить, что такое `void*`  
✔ почему нельзя разыменовывать  
✔ как правильно приводить тип  
✔ почему это небезопасно

---

## 1️⃣4️⃣ Итог

- `void*` — универсальный, но опасный указатель
    
- в C++ используется редко
    
- в современном C++ есть безопасные альтернативы
    
