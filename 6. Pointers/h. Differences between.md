**Различия между указателями и ссылками в C++**

---

## 1️⃣ Перевод темы

**Differences between pointers and references in C++ →  
Различия между указателями и ссылками в C++**

---

## 2️⃣ Общая идея

И **указатели**, и **ссылки** используются для обращения к объекту **не напрямую**,  
но они имеют **разную семантику и правила использования**.

---

## 3️⃣ Что такое ссылка (напоминание)

```cpp
int x = 10;
int& r = x;
```

- `r` — **псевдоним** `x`
    
- не является отдельным объектом
    
- всегда ссылается на один и тот же объект
    

---

## 4️⃣ Основные различия

|Критерий|Указатель (`int*`)|Ссылка (`int&`)|
|---|---|---|
|Может быть `nullptr`|✔|❌|
|Может быть неинициализирован|✔ (опасно)|❌|
|Можно переназначить|✔|❌|
|Требует разыменования|✔ (`*p`)|❌|
|Имеет собственный адрес|✔|❌ (логически)|
|Арифметика|✔|❌|
|Используется в массивах|✔|❌|
|Может быть `void*`|✔|❌|

---

## 5️⃣ Инициализация

### Указатель

```cpp
int x = 10;
int* p = &x;
```

### Ссылка

```cpp
int& r = x; // обязательна инициализация
```

---

## 6️⃣ Переназначение

### Указатель

```cpp
int a = 1, b = 2;
int* p = &a;
p = &b; // ✔
```

---

### Ссылка

```cpp
int& r = a;
r = b; // ❗ это присваивание значения, а не смена объекта
```

---

## 7️⃣ `nullptr` и безопасность

```cpp
int* p = nullptr; // ✔
int& r;           // ❌ невозможно
```

Ссылка **гарантирует наличие объекта**.

---

## 8️⃣ Передача в функции

### Через указатель

```cpp
void f(int* p) {
    if (p) { *p = 10; }
}
```

---

### Через ссылку

```cpp
void f(int& x) {
    x = 10;
}
```

✔ ссылка проще и безопаснее, если `nullptr` не нужен

---

## 9️⃣ Указатели и динамическая память

```cpp
int* p = new int(5);
delete p;
```

Ссылки:

- не владеют памятью
    
- не используются для `new/delete`
    

---

## 🔟 Внутреннее представление (важно для экзамена)

- указатель — **отдельная переменная с адресом**
    
- ссылка обычно реализуется как указатель под капотом
    
- но **семантически это разные сущности**
    

---

## 1️⃣1️⃣ Когда использовать что

### Используй ссылку, если:

✔ объект обязателен  
✔ не нужно менять, на что ссылаешься  
✔ передача параметров

---

### Используй указатель, если:

✔ возможен `nullptr`  
✔ нужна смена объекта  
✔ массивы и динамическая память  
✔ низкоуровневый код

---

## 1️⃣2️⃣ Частые ошибки студентов

❌ думают, что ссылку можно «перепривязать»  
❌ забывают проверять указатель на `nullptr`  
❌ используют указатель вместо ссылки без необходимости

---

## 1️⃣3️⃣ Экзаменационный акцент

Студент должен уметь:

✔ перечислить различия  
✔ привести примеры  
✔ выбрать правильный инструмент  
✔ объяснить семантику

---

## 1️⃣4️⃣ Итог

- ссылка — безопасный псевдоним
    
- указатель — мощный, но опасный инструмент
    
- в современном C++ **ссылки предпочтительнее**, где это возможно
    
