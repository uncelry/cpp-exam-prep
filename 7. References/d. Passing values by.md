Разбираем тему подробно.

---

## 🔹 Passing values by pointer and by reference — differences and use cases

### 🔸 Перевод темы

**Передача значений через указатель и через ссылку — различия и случаи использования**

---

## 1️⃣ Общая идея

В C++ есть два основных способа передавать объект **в функцию**, чтобы **функция могла изменить его значение**:

1. **Через указатель (pointer)**
    
2. **Через ссылку (reference)**
    

Оба способа позволяют работать **не с копией**, а с **оригинальным объектом**.

---

## 2️⃣ Передача через указатель

```cpp
void increment(int* p) {
    if (p != nullptr) {
        (*p)++;
    }
}

int main() {
    int a = 5;
    increment(&a);
    std::cout << a; // 6
}
```

### Особенности:

- Передаём **адрес объекта** (`&a`)
    
- В теле функции разыменовываем: `*p`
    
- Нужно **проверять на nullptr**
    
- Можно передавать **null**, что означает отсутствие объекта
    
- Можно переназначать: `p = &b;`
    

---

## 3️⃣ Передача через ссылку

```cpp
void increment(int& x) {
    x++;
}

int main() {
    int a = 5;
    increment(a);
    std::cout << a; // 6
}
```

### Особенности:

- Передаём **сам объект** через ссылку
    
- Разыменовывать не нужно (`x++` вместо `(*p)++`)
    
- Нельзя передать `nullptr`
    
- Ссылка **навсегда привязана к объекту**, её нельзя переназначить
    

---

## 4️⃣ Сравнение

|Критерий|Передача через указатель|Передача через ссылку|
|---|---|---|
|Синтаксис вызова|`increment(&a)`|`increment(a)`|
|Разыменование|нужно `*p`|не нужно|
|Может быть `nullptr`|✔|❌|
|Можно переназначить|✔|❌|
|Безопасность|меньше|выше|
|Использование|низкоуровневый код, массивы, динамическая память|большинство случаев в современном C++|

---

## 5️⃣ Когда использовать что

### ✅ Ссылки

- Функция **обязана работать с объектом**
    
- Не нужно проверять на `nullptr`
    
- Более безопасно и читабельно
    
- Передача аргументов в API, библиотеки
    
- Пример: `void print(const std::string& s);`
    

### ✅ Указатели

- Объект **необязателен** (может быть null)
    
- Работа с массивами, динамической памятью
    
- Когда нужно **переназначить объект внутри функции**
    

```cpp
void swap(int* &p1, int* &p2) { 
    int* temp = p1; 
    p1 = p2; 
    p2 = temp; 
}
```

- Такой код через ссылку на указатель (pointer reference) можно реализовать, но обычная ссылка не позволяет переназначить объект.
    

---

## 6️⃣ Примеры типичных ошибок

❌ Передача указателя без проверки на `nullptr`:

```cpp
void f(int* p) {
    *p = 10; // UB, если p == nullptr
}
```

❌ Попытка передать `nullptr` через ссылку:

```cpp
int& r = *nullptr; // UB, аварийно
```

❌ Перепутать `*p` и `p` при передаче/разыменовании

---

## 7️⃣ Краткое резюме для студента

> Передача по ссылке проще и безопаснее, когда объект **обязателен**.  
> Передача по указателю используется, когда объект **необязателен**, нужна возможность переназначить или работать с динамической памятью.

