**Модель памяти. Переменные и области видимости в C++**

---

## 1️⃣ Перевод темы

**Memory model → Модель памяти**  
**Variable → Переменная**  
**Global variables → Глобальные переменные**  
**Local variables → Локальные переменные**  
**Automatic storage duration → Автоматическое время хранения**  
**Global scope → Глобальная область видимости**  
**Storage classes → Классы хранения**

---

## 2️⃣ Модель памяти (общее представление)

**Модель памяти** описывает:

- как и где хранятся данные программы
    
- когда память выделяется и освобождается
    
- кто имеет доступ к данным
    

На базовом уровне (для экзамена):

- **код программы**
    
- **глобальные/статические данные**
    
- **стек (stack)** — локальные переменные
    
- **куча (heap)** — динамическая память (`new`, `delete`)
    

---

## 3️⃣ Что такое переменная

**Переменная** — это:

- именованная область памяти
    
- имеющая тип
    
- хранящая значение
    

```cpp
int x = 10;
```

Здесь:

- `int` — тип
    
- `x` — имя переменной
    
- `10` — значение
    

---

## 4️⃣ Объявление и определение переменной

### 🔹 Объявление (declaration)

Сообщает компилятору о существовании переменной.

```cpp
extern int x;
```

### 🔹 Определение (definition)

Выделяет память под переменную.

```cpp
int x = 10;
```

📌 Обычно объявление и определение совпадают.

---

## 5️⃣ Инициализация переменных

### 🔹 Без инициализации

```cpp
int x; // НЕ инициализирована (мусор)
```

### 🔹 С инициализацией

```cpp
int x = 5;     // копирующая
int y(5);     // прямая
int z{5};     // списковая (brace-init)

int x{3.14}; // Ошибка компиляции: narrowing-преобразование из double в int
```

📌 `z{5}` — самый безопасный способ (защита от narrowing).


---

## 6️⃣ Локальные переменные

### 🔹 Определение

Локальные переменные объявляются внутри функций или блоков.

```cpp
void f() {
    int x = 10;
}
```

### 🔹 Свойства

- область видимости: внутри блока
    
- время жизни: пока выполняется блок
    
- обычно хранятся в стеке
    
- **автоматическое время хранения**
    

---

## 7️⃣ Автоматическое время хранения (Automatic storage duration)

Переменные:

- создаются при входе в блок
    
- уничтожаются при выходе из блока
    

```cpp
void f() {
    int x = 5; // automatic
}
```

📌 Это поведение **по умолчанию** для локальных переменных.

---

## 8️⃣ Глобальные переменные

### 🔹 Определение

Объявлены **вне всех функций**.

```cpp
int globalVar = 100;
```

### 🔹 Свойства

- область видимости: весь файл (или программа)
    
- время жизни: вся программа
    
- инициализируются **до `main()`**
    
- хранятся в статической памяти
    

---

## 9️⃣ Различия между глобальными и локальными переменными

|Характеристика|Локальная|Глобальная|
|---|---|---|
|Область видимости|Блок|Вся программа|
|Время жизни|Время выполнения блока|Вся программа|
|Хранение|Стек|Статическая память|
|Инициализация|Не по умолчанию|По умолчанию = 0|
|Безопасность|Выше|Ниже|

---

## 🔟 Глобальная область видимости (Global scope)

Все имена, объявленные вне функций, находятся в **глобальной области видимости**.

```cpp
int x; // global scope

int main() {
    x = 5;
}
```

---

## 1️⃣1️⃣ Классы хранения (Storage classes)

Классы хранения определяют:

- **время жизни**
    
- **область видимости**
    
- **линковку**
    

---

### 🔹 `auto`

По умолчанию для локальных переменных (ключевое слово редко используется).

```cpp
auto int x = 5; // устарело
```

---

### 🔹 `static`

Меняет время хранения.

#### Локальная `static`

```cpp
void counter() {
    static int x = 0;
    x++;
}
```

- создаётся один раз
    
- сохраняет значение между вызовами
    

---

#### Глобальная `static`

```cpp
static int g = 10;
```

- видна только в текущем файле
    
- внутренняя линковка
    

---

### 🔹 `extern`

Объявление глобальной переменной, определённой в другом файле.

```cpp
extern int g;
```

---

### 🔹 `register` (устарело)

Подсказка компилятору хранить переменную в регистре.

```cpp
register int x;
```

📌 В C++20 не используется.

---

## 1️⃣2️⃣ Частые ошибки

❌ использование неинициализированных локальных переменных  
❌ злоупотребление глобальными переменными  
❌ непонимание `static`  
❌ путаница между объявлением и определением

---

## 1️⃣3️⃣ Итог

Надо понимать:

✔ что такое переменная  
✔ как объявляется и инициализируется  
✔ разницу между локальными и глобальными  
✔ что такое автоматическое время хранения  
✔ глобальную область видимости  
✔ назначение `static` и `extern`
