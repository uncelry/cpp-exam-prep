**Правила области видимости в C++ (Scope rules)**

---

## 1️⃣ Перевод темы

**Scope rules → Правила области видимости (область действия имён)**

---

## 2️⃣ Что такое область видимости (scope)

**Область видимости** — это часть программы, где имя:

- переменной
    
- функции
    
- типа
    
- класса
    
- пространства имён
    

является **доступным и видимым**.

Проще:

> scope отвечает на вопрос: **«где я могу использовать это имя?»**

---

## 3️⃣ Основные виды областей видимости в C++

### ✔ глобальная

### ✔ область пространства имён

### ✔ область функции

### ✔ блочная область

### ✔ область класса

### ✔ область цикла / условного оператора

---

## 4️⃣ Глобальная область видимости (Global scope)

### 🔹 Определение

Все имена, объявленные **вне функций и классов**, находятся в глобальной области видимости.

```cpp
int x = 10;

int main() {
    x = 5;
}
```

### 🔹 Доступ

- доступна из всего файла
    
- может быть доступна из других файлов через `extern`
    

---

## 5️⃣ Область пространства имён (Namespace scope)

```cpp
namespace math {
    int x = 10;
}
```

Доступ:

```cpp
math::x
```

📌 Используется для избежания конфликтов имён.

---

## 6️⃣ Область функции (Function scope)

### 🔹 Особенность

В C++ **только метки (`label`)** имеют function scope.

```cpp
void f() {
label:
    int x = 10;
    goto label;
}
```

📌 Остальные имена внутри функции имеют **блочную область**.

---

## 7️⃣ Блочная область видимости (Block scope)

### 🔹 Определение

Имена, объявленные внутри `{}`.

```cpp
if (true) {
    int x = 5;
}
// x здесь недоступна
```

Сюда входят:

- `if`, `else`
    
- `for`, `while`
    
- любые `{ }`
    

---

## 8️⃣ Область цикла

```cpp
for (int i = 0; i < 10; i++) {
    // i доступна
}
// i недоступна
```

📌 Переменные в заголовке цикла имеют область цикла.

---

## 9️⃣ Область класса (Class scope)

```cpp
class A {
public:
    int x;
    void f();
};
```

- `x` и `f` видны внутри класса
    
- доступ снаружи через объект или `::`
    

```cpp
A a;
a.x;
```

---

## 🔟 Область параметров функции

```cpp
void f(int x) {
    // x — параметр
}
```

- параметры имеют область тела функции
    

---

## 1️⃣1️⃣ Скрытие имён (Name hiding / Shadowing)

### 🔹 Пример

```cpp
int x = 10;

void f() {
    int x = 5; // скрывает глобальный x
    std::cout << x; // 5
}
```

📌 Внутреннее имя **скрывает внешнее**, но не уничтожает его.

---

## 1️⃣2️⃣ Оператор разрешения области видимости `::`

### 🔹 Доступ к глобальной переменной

```cpp
int x = 10;

void f() {
    int x = 5;
    std::cout << ::x; // 10
}
```

---

## 1️⃣3️⃣ Вложенные области видимости

```cpp
int x = 1;

void f() {
    int x = 2;
    {
        int x = 3;
        std::cout << x; // 3
    }
}
```

📌 Всегда выбирается **ближайшая область**.

---

## 1️⃣4️⃣ Область видимости `switch`

```cpp
switch (n) {
case 1:
    int x = 5; // ошибка без {}
    break;
}
```

Правильно:

```cpp
case 1: {
    int x = 5;
    break;
}
```

---

## 1️⃣5️⃣ `using` и область видимости

```cpp
using std::cout;
```

- имя видимо с места объявления до конца области
    

---

## 1️⃣6️⃣ Частые ошибки 

❌ ожидание, что переменная живёт вне блока  
❌ забывают, что `for(int i...)` ограничивает область  
❌ путаница между глобальной и namespace scope  
❌ ошибки в `switch`

---

## 1️⃣7️⃣ Итог

Надо понимать:

✔ что такое область видимости  
✔ основные виды scope в C++  
✔ правило ближайшей области  
✔ скрытие имён  
✔ использование `::`  
✔ ограничения `switch`
