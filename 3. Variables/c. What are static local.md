**Статические локальные переменные в C++ (Static local variables)**

---

## 1️⃣ Перевод темы

**Static local variables → Статические локальные переменные**

---

## 2️⃣ Что такое статическая локальная переменная

**Статическая локальная переменная** — это переменная, которая:

- объявлена **внутри функции или блока**
    
- имеет ключевое слово `static`
    
- **сохраняет своё значение между вызовами функции**
    

Пример:

```cpp
void counter() {
    static int x = 0;
    x++;
    std::cout << x << '\n';
}
```

---

## 3️⃣ Как они работают

### 🔹 Время жизни

- создаётся **один раз**
    
- существует **в течение всей программы**
    

📌 В отличие от обычной локальной переменной, которая создаётся при каждом вызове функции.

---

### 🔹 Область видимости

- **локальная** — только внутри функции или блока
    
- недоступна извне
    

---

## 4️⃣ Где хранится статическая локальная переменная

- хранится в **статической области памяти**
    
- не в стеке
    

То есть:

- не уничтожается при выходе из функции
    
- значение сохраняется
    

---

## 5️⃣ Инициализация статических локальных переменных

### 🔹 Инициализация выполняется один раз

```cpp
void f() {
    static int x = 10;
}
```

- инициализируется **при первом входе в функцию**
    
- не при каждом вызове
    

---

### 🔹 Гарантия C++11+

Инициализация:

- **потокобезопасна**
    
- выполняется ровно один раз даже в многопоточном коде
    

---

## 6️⃣ Сравнение: обычная vs static локальная

|Характеристика|Обычная локальная|static локальная|
|---|---|---|
|Область видимости|Локальная|Локальная|
|Время жизни|Вызов функции|Вся программа|
|Хранение|Стек|Статическая память|
|Инициализация|Каждый вызов|Один раз|

---

## 7️⃣ Пример для понимания

```cpp
void test() {
    int a = 0;
    static int b = 0;
    a++;
    b++;
    std::cout << a << " " << b << '\n';
}

int main() {
    test();
    test();
    test();
}
```

Вывод:

```
1 1
1 2
1 3
```

---

## 8️⃣ Зачем используются static локальные переменные

✔ счётчики вызовов  
✔ кеширование значений  
✔ ленивую инициализацию  
✔ сохранение состояния функции

---

## 9️⃣ Ограничения и опасности

❌ усложняют тестирование  
❌ скрытое состояние  
❌ потенциальные проблемы в многопоточности (если есть запись)

---

## 🔟 Static локальная vs глобальная

|static локальная|глобальная|
|---|---|---|
|Область видимости|Локальная|Глобальная|
|Время жизни|Вся программа|Вся программа|
|Доступ|Только функция|Везде|
|Инкапсуляция|Лучше|Хуже|

📌 `static` локальная безопаснее глобальной.

---

## 1️⃣1️⃣ Частые ошибки студентов

❌ ожидание, что значение обнуляется при каждом вызове  
❌ путаница с обычной локальной  
❌ забывают, что инициализация один раз  
❌ не понимают, где хранится переменная

---

## 1️⃣2️⃣ Итог для студента

Студент должен понимать:

✔ что такое static локальная переменная  
✔ чем она отличается от обычной  
✔ где она хранится  
✔ как и когда инициализируется  
✔ зачем используется
