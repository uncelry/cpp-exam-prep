**Выражения и инструкции. Категории значений в C++**

---

## 1️⃣ Перевод темы

**Expressions / statements → Выражения и инструкции (операторы)**  
**Value categories → Категории значений**  
**l-value, r-value → lvalue и rvalue**  
**Increment and decrement → Инкремент и декремент**

---

## 2️⃣ Выражения и инструкции

### 🔹 Выражение (expression)

**Выражение** — это конструкция, которая:

- вычисляется
    
- имеет **значение**
    
- имеет **тип**
    
- может иметь **категорию значения**
    

Примеры:

```cpp
x
x + 1
func()
```

---

### 🔹 Инструкция / оператор (statement)

**Инструкция** — это завершённая команда программы.

Примеры:

```cpp
int x = 5;
x++;
if (x > 0) { }
```

📌 Выражения могут быть частью инструкций, но не наоборот.

---

## 3️⃣ Категории значений (Value categories)

В базовом курсе важно понимать:

- **lvalue**
    
- **rvalue**
    

(Формально в C++ их больше, но для экзамена этого достаточно.)

---

## 4️⃣ lvalue (левостороннее значение)

### 🔹 Определение

**lvalue** — выражение, которое:

- указывает на **объект в памяти**
    
- имеет **адрес**
    
- может стоять **слева от =**
    

### 🔹 Примеры

```cpp
int x = 10;
x = 5;     // x — lvalue
```

```cpp
int& ref = x; // допустимо
```

---

## 5️⃣ rvalue (правостороннее значение)

### 🔹 Определение

**rvalue** — временное значение, которое:

- **не имеет собственного имени**
    
- обычно **не имеет адреса**
    
- не может быть слева от =
    

### 🔹 Примеры

```cpp
10
x + 1
func()
```

```cpp
x + 1 = 5; // ошибка
```

---

## 6️⃣ Сравнение lvalue и rvalue

|lvalue|rvalue|
|---|---|---|
|Есть адрес|✔|❌|
|Можно присваивать|✔|❌|
|Можно взять ссылку `T&`|✔|❌|
|Временное|❌|✔|

---

## 7️⃣ Инкремент и декремент

### 🔹 Префиксные формы

```cpp
++x;
--x;
```

- сначала изменяют
    
- потом возвращают значение
    
- результат — **lvalue**
    

---

### 🔹 Постфиксные формы

```cpp
x++;
x--;
```

- сначала возвращают старое значение
    
- потом изменяют
    
- результат — **rvalue**
    

---

## 8️⃣ Пример различий

```cpp
int x = 5;

int a = ++x; // x = 6, a = 6
int b = x++; // b = 6, x = 7
```

---

## 9️⃣ Категория значения инкремента

```cpp
++x = 10; // корректно (lvalue)
x++ = 10; // ошибка (rvalue)
```

---

## 🔟 Инкремент и производительность

Для пользовательских типов:

```cpp
++it; // предпочтительнее
it++; // может быть дороже
```

Потому что постфиксная версия часто создаёт временный объект.

---

## 1️⃣1️⃣ Частые ошибки

❌ путать выражение и инструкцию  
❌ считать, что `x++` возвращает lvalue  
❌ не понимать разницу `++x` и `x++`  
❌ пытаться присваивать rvalue

---

## 1️⃣2️⃣ Мини-тест

```cpp
int x = 1;
(x++)++;
```

❌ Ошибка: `(x++)` — rvalue

---

## 1️⃣3️⃣ Итог

Надо понимать:

✔ разницу между выражением и инструкцией  
✔ что такое lvalue и rvalue  
✔ какие выражения можно присваивать  
✔ разницу между `++x` и `x++`  
✔ категорию значения результата инкремента
