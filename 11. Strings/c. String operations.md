Разбираем тему подробно, в формате для подготовки к экзамену.

---

## 🔹 String operations: concatenation, search, replacement

### 🔸 Перевод темы

**Операции со строками: конкатенация, поиск, замена**

---

## 1️⃣ Какие строки имеются в виду

В C++ операции со строками могут выполняться:

- над **`std::string`** (рекомендуемый вариант)
    
- над **C-строками (`char*`)** — реже, небезопасно
    

На экзамене обычно ожидают знание **`std::string`**.

---

## 2️⃣ Конкатенация строк (объединение)

### 🔹 Оператор `+`

```cpp
std::string a = "Hello";
std::string b = " world";

std::string c = a + b;
```

Также можно:

```cpp
std::string c = a + " world";
```

❗ Но:

```cpp
"Hello" + "world"; // ❌ ошибка (оба — литералы)
```

---

### 🔹 Оператор `+=`

```cpp
std::string s = "Hello";
s += " ";
s += "C++";
```

---

### 🔹 Метод `append`

```cpp
std::string s = "Hello";
s.append(" world");
```

---

### 🔹 Производительность

- `+=` и `append` **эффективнее**, чем многократный `+`
    
- `+` часто создаёт временные объекты
    

---

## 3️⃣ Поиск в строке

### 🔹 Метод `find`

```cpp
std::string s = "Hello world";

size_t pos = s.find("world");
if (pos != std::string::npos) {
    std::cout << "Найдено с позиции " << pos << std::endl;
}
```

- Возвращает позицию
    
- Если не найдено → `std::string::npos`
    

---

### 🔹 Поиск символа

```cpp
size_t pos = s.find('o');
```

---

### 🔹 Поиск с позиции

```cpp
s.find("o", 5);
```

---

### 🔹 Обратный поиск

```cpp
s.rfind("o");
```

---

## 4️⃣ Извлечение подстроки

(часто идёт вместе с поиском)

```cpp
std::string sub = s.substr(6, 5); // "world"
```

---

## 5️⃣ Замена в строке

### 🔹 Метод `replace`

```cpp
std::string s = "Hello world";

s.replace(6, 5, "C++");
```

Результат:

```
Hello C++
```

Синтаксис:

```cpp
replace(позиция, длина_замены, новая_строка)
```

---

### 🔹 Замена по результату find

```cpp
std::string s = "I like Java";
size_t pos = s.find("Java");

if (pos != std::string::npos) {
    s.replace(pos, 4, "C++");
}
```

---

## 6️⃣ Удаление части строки

(технически тоже операция изменения)

```cpp
s.erase(5, 6);
```

Или:

```cpp
s.erase(pos);
```

---

## 7️⃣ Вставка строки

```cpp
std::string s = "Hello world";
s.insert(6, "beautiful ");
```

---

## 8️⃣ Аналоги для C-строк (кратко)

```cpp
strcat(a, b);
strstr(a, b);
strcmp(a, b);
```

❗ Опасно:

- нет проверки границ
    
- легко переполнить буфер
    

На экзамене: **знать, но не рекомендовать**

---

## 9️⃣ Типичные ошибки

1. Забывают проверить `npos`
    
2. Путают индексы и длины
    
3. Используют `+` в цикле → плохая производительность
    
4. Пытаются модифицировать строковый литерал
    

---

## 🔟 Кратко для экзамена

- Конкатенация: `+`, `+=`, `append`
    
- Поиск: `find`, `rfind`
    
- Проверка: `npos`
    
- Замена: `replace`
    
- Все индексы — `size_t`
    
- `std::string` — предпочтительный вариант
    
