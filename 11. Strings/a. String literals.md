Разбираем тему подробно, в формате для подготовки к экзамену.

---

## 🔹 String literals. Strings. String storage

### 🔸 Перевод темы

**Строковые литералы. Строки. Хранение строк в памяти**

---

## 1️⃣ Строковой литерал (string literal)

### 🔹 Определение

**Строковой литерал** — это **последовательность символов в двойных кавычках**:

```cpp
"Hello, world"
```

Тип строкового литерала в C++:

```cpp
const char[N]
```

где `N` — количество символов **+ 1 для `'\0'`** (нулевого терминатора).

Пример:

```cpp
"Hi"
```

В памяти:

```
'H' 'i' '\0'
```

Размер массива = 3.

---

## 2️⃣ Где хранятся строковые литералы

- Строковые литералы:
    
    - хранятся в **статической области памяти**
        
    - существуют **всю жизнь программы**
        
    - **нельзя изменять** (в C++ они `const`)
        

❌ Ошибка:

```cpp
char* p = "Hello";   // запрещено в современном C++
p[0] = 'h';          // неопределённое поведение
```

✅ Правильно:

```cpp
const char* p = "Hello";
```

---

## 3️⃣ C-строки (C-style strings)

### 🔹 Определение

**C-строка** — это массив символов `char`, оканчивающийся `'\0'`.

Пример:

```cpp
char str[] = "Hello";
```

Фактически:

```cpp
{'H','e','l','l','o','\0'}
```

---

### 🔹 Основные особенности C-строк

- Длина определяется по `'\0'`
    
- Нет автоматической проверки границ
    
- Часто используются функции из `<cstring>`
    

Примеры:

```cpp
strlen(str);
strcpy(dest, src);
strcmp(a, b);
```

---

## 4️⃣ std::string — строка в C++

### 🔹 Определение

**`std::string`** — это класс стандартной библиотеки (`<string>`), который:

- хранит строку динамически
    
- управляет памятью автоматически
    
- безопаснее и удобнее C-строк
    

Пример:

```cpp
#include <string>

std::string s = "Hello";
s += " world";
```

---

## 5️⃣ Хранение std::string в памяти

Внутри `std::string`:

- есть указатель на динамический буфер
    
- хранится размер строки
    
- хранится ёмкость (`capacity`)
    

```cpp
std::string s = "Hello";
```

Упрощённо:

```
stack:   s (объект)
heap:    'H''e''l''l''o''\0'
```

### 🔹 Small String Optimization (SSO)

Многие реализации используют **SSO**:

- короткие строки хранятся **внутри объекта**
    
- без выделения памяти в куче
    

Это:

- быстрее
    
- без `new/delete`
    

(Важно: стандарт не гарантирует, но на практике почти всегда есть)

---

## 6️⃣ Сравнение: string literal / C-string / std::string

|Характеристика|String literal|C-строка|std::string|
|---|---|---|---|
|Изменяемость|❌ нет|✅ да|✅ да|
|Время жизни|вся программа|область видимости|управляется объектом|
|Безопасность|высокая|низкая|высокая|
|'\0'|есть|обязателен|логически есть|
|Изменение размера|❌|❌|✅|
|Рекомендуется|редко напрямую|legacy / низкий уровень|✅ да|

---

## 7️⃣ Строковые литералы разных типов (C++11+)

```cpp
u8"UTF-8"
u"UTF-16"
U"UTF-32"
L"wide string"
```

Типы:

- `char8_t`
    
- `char16_t`
    
- `char32_t`
    
- `wchar_t`
    

---

## 8️⃣ Частые ошибки

1. Попытка изменить строковый литерал
    
2. Забытый `'\0'` в C-строке
    
3. Переполнение буфера при работе с `char[]`
    
4. Использование `char*` вместо `const char*`
    

---

## 9️⃣ Кратко для экзамена

- **Строковой литерал** — `const char[N]`, хранится в статической памяти
    
- **C-строка** — массив `char` с `'\0'`
    
- **std::string** — класс, который безопасно управляет строками
    
- Литералы нельзя менять
    
- `std::string` — предпочтительный вариант в C++
    
