**Числа с плавающей точкой (Floating-point numbers).  
Чем отличаются целочисленные и вещественные типы, в том числе по представлению в памяти (IEEE 754).**

---

## 1️⃣ Перевод темы

**Floating-point numbers → Числа с плавающей точкой (вещественные числа)**

---

## 2️⃣ Целочисленные vs вещественные типы — общая идея

### 🔹 Целочисленные типы (`int`, `long`, …)

- Хранят **только целые значения**
- Представляются в памяти как **фиксированное бинарное число**
- Точные (нет округлений)
- Ограниченный диапазон

### 🔹 Вещественные типы (`float`, `double`, `long double`)

- Хранят **дробные значения**
- Представляются в формате **научной записи**
- Ограниченная точность
- Возможны ошибки округления

---

## 3️⃣ Вещественные типы в C++

Типичные размеры:

|Тип|Размер|Точность|
|---|---|---|
|`float`|4 байта (32 бита)|~7 значащих цифр|
|`double`|8 байт (64 бита)|~15–16 значащих цифр|
|`long double`|8 / 12 / 16 байт|зависит от платформы|

---

## 4️⃣ Представление целых чисел в памяти

Целые числа:

- хранятся **непосредственно в двоичном виде**
- для отрицательных используется **дополнительный код (two’s complement)**

Пример (`int8_t`):

```
5   → 00000101
-5  → 11111011
```

Все биты — это само число.

---

## 5️⃣ Представление вещественных чисел

Вещественные числа хранятся **не как обычное число**, а в виде:

```
± M × 2^E
```

Где:

- **M** — мантисса (значащая часть)
- **E** — порядок (экспонента)

Это аналог научной записи:

```
123.45 = 1.2345 × 10^2
```

В компьютере — основание **2**, а не 10.

---

## 6️⃣ IEEE 754 — стандарт представления

Большинство современных систем используют стандарт **IEEE 754**.

---

### 🔹 Формат `float` (32 бита)

```
[ знак | экспонента | мантисса ]
   1        8            23
```

- **Знак (1 бит)**  
    0 → положительное  
    1 → отрицательное
    
- **Экспонента (8 бит)**  
    Хранится со смещением (bias = 127)
    
- **Мантисса (23 бита)**  
    Хранит дробную часть  
    Ведущий `1` не хранится (скрытый бит)
    

---

### 🔹 Формат `double` (64 бита)

```
[ знак | экспонента | мантисса ]
   1       11            52
```

Bias экспоненты = **1023**

---

## 7️⃣ Пример представления числа

Возьмём число:

```
10.25
```

### Шаг 1. В двоичном виде:

```
10.25 = 1010.01₂
```

### Шаг 2. Нормализация:

```
1.01001 × 2^3
```

- Мантисса: `1.01001`
- Экспонента: `3`
### Шаг 3. В IEEE 754:

- Знак = 0
- Экспонента = 3 + 127 = 130 → `10000010`
- Мантисса = `01001000000000000000000`

---

## 8️⃣ Почему возникают ошибки округления

Некоторые десятичные дроби **не имеют точного двоичного представления**.

Пример:

```cpp
double x = 0.1;
```

0.1 в двоичной системе:

```
0.00011001100110011... (бесконечно)
```

Поэтому хранится **приближённое значение**.

---

## 9️⃣ Классический пример ошибки

```cpp
double a = 0.1;
double b = 0.2;
double c = a + b;

std::cout << (c == 0.3);
```

Результат:

```
false
```

Почему?  
Потому что:

```
0.1 + 0.2 ≠ 0.3 (в двоичном приближении)
```

---

## 🔟 Особые значения IEEE 754

Вещественные типы поддерживают:

- `+inf`, `-inf` — переполнение
- `NaN` — не число (0/0, sqrt(-1))
- `-0` — отрицательный ноль

Пример:

```cpp
double x = 1.0 / 0.0;  // inf
double y = 0.0 / 0.0;  // NaN
```

---

## 1️⃣1️⃣ Сравнение int и float (ключевая таблица)

|Характеристика|int|float/double|
|---|---|---|
|Дробные числа|❌|✅|
|Точность|точная|приближённая|
|Переполнение|UB / модуль|inf|
|Представление|обычное бинарное|мантисса + экспонента|
|Сравнение|точное|с погрешностью|

---

## 1️⃣2️⃣ Итог

Надо понимать:

✔ целые числа хранятся напрямую  
✔ вещественные — в формате IEEE 754  
✔ `float` и `double` — приближённые  
✔ мантисса отвечает за точность  
✔ экспонента — за масштаб  
✔ ошибки округления — нормальное явление  
✔ вещественные числа нельзя сравнивать через ==

