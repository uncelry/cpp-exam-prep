Integer types (int, long, long long, short). signed/unsigned modifiers. Operations with integer types. Fixed-width integer types. What are the differences between signed and unsigned integers, including from the perspective of memory representation? Provide an example.

## 1️⃣ Перевод темы

**Integer types → Целочисленные типы данных**  
(включая `int`, `short`, `long`, `long long`, модификаторы `signed/unsigned`, фиксированные типы и представление в памяти)

---

## 2️⃣ Основные целочисленные типы в C++

Целочисленные типы используются для хранения **целых чисел без дробной части**.

Типичные размеры на современных 64-бит системах:

|Тип|Минимум по стандарту|Обычно|
|---|---|---|
|`short` / `short int`|≥ 16 бит|2 байта|
|`int`|≥ 16 бит|4 байта|
|`long`|≥ 32 бит|4 байта (Windows), 8 байт (Linux)|
|`long long`|≥ 64 бит|8 байт|

⚠️ Размеры зависят от компилятора и платформы! C++ гарантирует **минимальные**, а не фиксированные размеры.

---

## 3️⃣ signed и unsigned

Каждый целочисленный тип может быть:

- **signed** (со знаком)
- **unsigned** (без знака)

Если не указано — по умолчанию `signed`.

Примеры:

```signed int a;   // обычно просто int 
unsigned int b; 
short s; 
unsigned short us; 
long long ll; 
unsigned long long ull;
```

---

## 4️⃣ Диапазоны значений

(типично при 32-битном int)

|Тип|Диапазон|
|---|---|
|`int`|−2 147 483 648 … 2 147 483 647|
|`unsigned int`|0 … 4 294 967 295|
|`short`|−32768 … 32767|
|`unsigned short`|0 … 65535|
|`long long`|примерно −9e18 … 9e18|
|`unsigned long long`|0 … ~18e18|

---

## 5️⃣ Представление в памяти (главное отличие signed vs unsigned)

### 🔹 unsigned

Хранит **только неотрицательные числа**.  
Все биты используются для значения.

Пример `uint8_t` (1 байт):

`00000000 = 0 00000001 = 1 ... 11111111 = 255`

### 🔹 signed

Использует **метод дополнительного кода (two’s complement)**.  
Старший бит — знак:

- 0 → положительное
- 1 → отрицательное

Пример `int8_t`:

```
00000000 = 0 
00000001 = 1 
... 
01111111 = 127 
10000000 = -128 
10000001 = -127 
... 
11111111 = -1
```

То есть отрицательные числа хранятся как:

`-|X| = 2^N − X`

**Дополнительный код** – это система представления знаковых целых, упрощающая выполнение двоичной арифметики. Чтобы найти дополнительный код двоичного числа, вы инвертируете все его биты (изменяя 0 на 1 и наоборот), после чего прибавляете к результату единицу. Это представление упрощает построение вычислительных устройств, позволяя выполнять сложение и вычитание при помощи одной и тоже схемы, поскольку вычитание числа равнозначно прибавлению его дополнительного кода. К тому же этот приём позволяет эффективно обрабатывать изменение знаков и нулевые значения, в связи с чем он так широко используется в вычислительных системах.

---

## 6️⃣ Важный пример различий

```
signed int a = -1; 
unsigned int b = 4294967295;  
std::cout << a << std::endl; 
std::cout << b << std::endl;
```

Оба хранятся как:

`11111111 11111111 11111111 11111111`

но:

- `signed` интерпретирует это как **−1**
- `unsigned` — как **большое положительное число**

---

## 7️⃣ Опасность смешивания signed и unsigned

👇 Это очень частая ошибка студентов!

```
int a = -5; 
unsigned int b = 10;  
if (a < b)     
	std::cout << "True"; 
else     
	std::cout << "False";
```

Результат: **False!**

Почему?  
`a` автоматически приводится к unsigned → становится огромным положительным числом.

---

## 8️⃣ Переполнение

### signed overflow → неопределённое поведение (UB)

```
int x = 2147483647; 
x++;
```

Результат формально **не определён стандартом**.

---

### unsigned overflow → безопасная арифметика по модулю

```
unsigned int x = 0; 
x--;
```

Результат:

`4294967295`

То есть происходит “кольцевое” переполнение:

`(0 - 1) mod 2^32`

---

## 9️⃣ Операции с целыми типами

Разрешены:

- `+ - * / %`
- битовые операции: `& | ^ ~ << >>`
- сравнения
- инкремент/декремент `++ --`

Особенности:

- деление `/` — целочисленное (дробная часть отбрасывается)
- `%` работает только с целыми
    
Пример:

```
int a = 7 / 2;   // 3 
int b = 7 % 2;   // 1
```

---

## 🔟 Фиксированные целочисленные типы

Из `<cstdint>` — **гарантированный размер** на любой платформе.

`#include <cstdint>`

|Тип|Размер|
|---|---|
|`int8_t` / `uint8_t`|8 бит|
|`int16_t` / `uint16_t`|16 бит|
|`int32_t` / `uint32_t`|32 бита|
|`int64_t` / `uint64_t`|64 бита|

Также есть:

- `int_fastN_t` — самый быстрый ≥ N бит
- `int_leastN_t` — минимально возможный ≥ N бит

Пример:

```
int32_t x = 100; 
uint64_t big = 1000000000000ULL;
```

---

## 1️⃣1️⃣ Итог

Надо понимать:

✔ разные целочисленные типы имеют разный размер и диапазон  
✔ `signed` хранит отрицательные числа (двойное дополнение)  
✔ `unsigned` только ≥ 0 и работает по модулю  
✔ нельзя бездумно смешивать signed и unsigned  
✔ переполнение signed — опасно, unsigned — по модулю  
✔ для надёжных программ используйте **fixed-width типы (`int32_t`, `uint64_t`)**
