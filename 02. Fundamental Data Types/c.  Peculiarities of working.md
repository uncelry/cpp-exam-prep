Peculiarities of working with large numbers. What is overflow? Why does it occur?

## 1️⃣ Перевод темы

**Peculiarities of working with large numbers → Особенности работы с большими числами**  
**What is overflow? Why does it occur? → Что такое переполнение и почему оно происходит?**

---

## 2️⃣ В чём особенность работы с большими числами?

В C++ **каждый числовой тип имеет ограниченный диапазон значений**, потому что он хранится в фиксированном количестве бит.

Примеры типичных диапазонов (64-битная система):

- `int` → от −2 147 483 648 до 2 147 483 647
- `unsigned int` → от 0 до 4 294 967 295
- `long long` → примерно до ±9 * 10¹⁸
- `uint64_t` → до 18 * 10¹⁸

Когда число выходит за пределы допустимого диапазона — возникает **переполнение**.

---

## 3️⃣ Что такое переполнение (overflow)?

**Переполнение** — это ситуация, когда результат вычисления не помещается в отведённое количество бит для типа данных.

Проще:

> Вы положили в коробку число, которое в неё не помещается → оно “ломается”.

---

## 4️⃣ Почему переполнение происходит?

Причины:  
1️⃣ число слишком большое для типа  
2️⃣ результат выражения больше диапазона  
3️⃣ арифметика выполняется в меньшем типе  
4️⃣ программист забыл, что:

- размеры типов ограничены
- `unsigned` и `signed` ведут себя по-разному

---

## 5️⃣ Что происходит при переполнении?

### 🔹 1. Переполнение **unsigned**

Происходит арифметика **по модулю** (гарантировано стандартом):

```
unsigned int x = 0; 
x--;   // "уходим ниже минимума"
```

Результат:

`4294967295   (для 32-bit unsigned)`

То есть:

`(0 - 1) mod 2^32`

---

### 🔹 2. Переполнение **signed**

⚠️ Важно:  
**Signed overflow — неопределённое поведение (UB)**  
Стандарт НЕ гарантирует результат.

Пример:

```
int x = 2147483647;  // max 
int x++;
```

Может:

- перейти в отрицательное
- остаться тем же
- дать мусор
- программа может работать странно

То есть “что угодно”, формально поведение не определено стандартом.

---

## 6️⃣ Пример

```
int a = 2147483647; 
int b = a + 1; 
std::cout << b;
```

На большинстве систем вы увидите:

`-2147483648`

но это **случайность платформы**, а не гарантия.

---

## 7️⃣ Большие числа и умножение

Даже если операнды помещаются — результат может не поместиться:

```
int a = 50000; 
int b = 50000;  
int c = a * b;  // переполнение
```

---

## 8️⃣ Большие числа и промежуточные вычисления

Переполнение может произойти даже если конечный тип “большой”, но вычисление сначала идёт в меньшем типе:

```
int a = 1000000; 
int b = 1000000;  
long long c = a * b;   // overflow всё равно в int стадии!
```

Чтобы избежать:

`long long c = 1LL * a * b;`

---

## 9️⃣ Переполнение у `float/double`

Там другая проблема:

- переполнение → `inf`
- потеря точности
- underflow (слишком маленькое число → 0)
- ошибки округления

Но **нет UB**, просто математические последствия.

---

## 🔟 Как избежать переполнения?

✔ использовать более широкий тип:

- `long long`
- `uint64_t`  
    ✔ применять фиксированные типы `<cstdint>`  
    ✔ проверять диапазоны:
	`#include <limits>  if (a > std::numeric_limits<int>::max() - b) { ... }`
✔ в современных компиляторах есть встроенные проверки:
- `__int128` (GCC/Clang)
- `std::overflow_error` (в библиотеках BigInt)  
    ✔ для реально больших чисел:
- `boost::multiprecision::cpp_int`
- библиотеки arbitrary precision  
    ✔ не смешивать signed и unsigned

---

## 1️⃣1️⃣ Вывод

Надо понимать:

- числа занимают фиксированное количество бит
- превышение диапазона приводит к переполнению
- `unsigned` → безопасно по модулю
- `signed` → **неопределённое поведение**
- большие числа требуют продуманного выбора типа
- особенно опасны умножения и промежуточные вычисления

