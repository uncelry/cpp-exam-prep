**Квалификаторы `const` и `volatile` в C++**

---

## 1️⃣ Перевод темы

**The const qualifier → Квалификатор `const`**  
**The volatile qualifier → Квалификатор `volatile`**  
**Can a constant value be changed? → Может ли константное значение быть изменено?**

---

## 2️⃣ Квалификатор `const`

### 🔹 Что означает `const`

`const` говорит компилятору:

> значение **нельзя изменять через это имя**

```cpp
const int x = 10;
x = 5; // ошибка компиляции
```

📌 `const` — это **гарантия на уровне компилятора**, а не на уровне памяти.

---

## 3️⃣ `const` и переменные

```cpp
const int a = 5;
```

- `a` нельзя изменить
    
- обязательно инициализируется при объявлении
    

---

## 4️⃣ `const` и указатели

### 🔹 `const` относится к тому, что слева (правило «читай справа налево»)

#### Указатель на `const`

```cpp
const int* p = &x;
// *p = 5; // нельзя
// p = &y; // можно
```

#### `const` указатель

```cpp
int* const p = &x;
// *p = 5; // можно
// p = &y; // нельзя
```

#### `const` указатель на `const`

```cpp
const int* const p = &x;
```

---

## 5️⃣ `const` и ссылки

```cpp
const int& ref = x;
// ref = 5; // нельзя
```

Позволяет:

- ссылаться на временные объекты
    
- избегать копирования
    

---

## 6️⃣ `const` в функциях

### 🔹 Параметры

```cpp
void f(const int x);
```

### 🔹 Возвращаемое значение

```cpp
const int f();
```

(редко используется)

---

### 🔹 `const`-методы класса

```cpp
class A {
public:
    int x;
    int get() const {
        return x;
    }
};
```

Гарантирует:

- метод не изменяет состояние объекта
    

---

## 7️⃣ Можно ли изменить `const` значение?

### 🔹 Через обычный код — ❌ нельзя

```cpp
const int x = 10;
x = 5; // ошибка
```

---

### 🔹 Через `const_cast` — ⚠️ опасно

```cpp
const int x = 10;
int& y = const_cast<int&>(x);
y = 5; // UB (неопределённое поведение)
```

📌 Если объект был объявлен `const`, его изменение — **UB**.

✔ допустимо только если изначально объект **НЕ был `const`**.

---

## 8️⃣ Квалификатор `volatile`

### 🔹 Что означает `volatile`

`volatile` говорит компилятору:

> значение может измениться **вне контроля программы**

Пример:

```cpp
volatile int flag;
```

---

## 9️⃣ Зачем нужен `volatile`

✔ работа с аппаратными регистрами  
✔ взаимодействие с устройствами  
✔ сигналы  
✔ memory-mapped I/O

Компилятор:

- не кэширует значение
    
- не оптимизирует обращения
    

---

## 🔟 `volatile` ≠ thread-safety

❌ `volatile` **НЕ** делает код потокобезопасным  
✔ для многопоточности используют `std::atomic`

---

## 1️⃣1️⃣ `const` + `volatile`

```cpp
const volatile int status;
```

- `const` — нельзя менять из кода
    
- `volatile` — может меняться извне
    

---

## 1️⃣2️⃣ Сравнение `const` и `volatile`

|const|volatile|
|---|---|---|
|Запрещает изменение|✔|❌|
|Запрещает оптимизации|❌|✔|
|Гарантия компилятора|✔|✔|
|Используется для|неизменяемых данных|внешних изменений|

---

## 1️⃣3️⃣ Частые ошибки

❌ думать, что `const` = неизменяемая память  
❌ использовать `volatile` вместо `atomic`  
❌ менять `const` через `const_cast`  
❌ путать `const int*` и `int* const`

---

## 1️⃣4️⃣ Итог 

Надо понимать:

✔ смысл `const`  
✔ где и как он применяется  
✔ можно ли изменить `const` объект  
✔ зачем нужен `volatile`  
✔ почему `volatile` не для потоков
