**Указатели и `const`. Указатели на указатели**

---

## 1️⃣ Перевод темы

**Pointers and const. Pointers to pointers →  
Указатели и `const`. Указатели на указатели**

---

## 2️⃣ `const` и указатели — общая идея

`const` может относиться:

- либо к **данным**, на которые указывает указатель
    
- либо к **самому указателю**
    
- либо к **обоим сразу**
    

Золотое правило чтения:

> читать объявление **справа налево**.

---

## 3️⃣ Указатель на `const` данные

```cpp
const int* p;
int const* p; // то же самое
```

### 📌 Что означает

- **нельзя менять значение**
    
- **можно менять адрес**
    

```cpp
int a = 10, b = 20;
const int* p = &a;

*p = 5;   // ❌ нельзя
p = &b;   // ✔ можно
```

---

## 4️⃣ `const` указатель

```cpp
int* const p = &a;
```

### 📌 Что означает

- **можно менять значение**
    
- **нельзя менять адрес**
    

```cpp
int a = 10;
int* const p = &a;

*p = 20;  // ✔ можно
p = &b;   // ❌ нельзя
```

---

## 5️⃣ `const` указатель на `const` данные

```cpp
const int* const p = &a;
```

### 📌 Что означает

- нельзя менять ни адрес
    
- нельзя менять значение
    

---

## 6️⃣ Таблица для запоминания

|Объявление|Можно менять значение|Можно менять адрес|
|---|---|---|
|`const int* p`|❌|✔|
|`int* const p`|✔|❌|
|`const int* const p`|❌|❌|

---

## 7️⃣ Зачем нужен `const` с указателями

- защита от случайных изменений
    
- документирование намерений
    
- возможность передавать данные без копирования
    
- важно для интерфейсов функций
    

```cpp
void print(const int* arr, int size);
```

---

## 8️⃣ Указатели на указатели

### 📌 Что это такое

**Указатель на указатель** хранит адрес другого указателя.

```cpp
int x = 5;
int* p = &x;
int** pp = &p;
```

---

## 9️⃣ Визуализация

```
x   = 5
p   → &x
pp  → &p
```

---

## 🔟 Разыменование

```cpp
std::cout << **pp; // 5
```

- `*pp` → `p`
    
- `**pp` → `x`
    

---

## 1️⃣1️⃣ Изменение значения через указатель на указатель

```cpp
**pp = 10;
```

Теперь `x == 10`.

---

## 1️⃣2️⃣ Зачем нужны указатели на указатели

- изменение указателя внутри функции
    
- динамические массивы
    
- таблицы указателей
    
- реализация сложных структур данных
    

Пример:

```cpp
void allocate(int** p) {
    *p = new int(5);
}
```

---

## 1️⃣3️⃣ `const` и указатели на указатели (частая ловушка)

```cpp
const int** pp; // ❌ опасно
```

Почему:

- нарушает `const`-безопасность
    

Правильно:

```cpp
const int* const* pp;
```

---

## 1️⃣4️⃣ Частые ошибки

❌ путают, к чему относится `const`  
❌ забывают правило чтения справа налево  
❌ разыменовывают `nullptr`  
❌ теряются при `**pp`

---

## 1️⃣5️⃣ Экзаменационный минимум


✔ читать сложные объявления  
✔ объяснять различия `const int*` и `int* const`  
✔ работать с `int**`  
✔ объяснять назначение

---

## 1️⃣6️⃣ Итог

- `const` может относиться к данным или указателю
    
- указатели на указатели — цепочка адресов
    
- важно соблюдать `const`-корректность
    
