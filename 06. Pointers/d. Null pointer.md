**Нулевой указатель (Null pointer) в C++**

---

## 1️⃣ Перевод темы

**Null pointer → Нулевой указатель**

---

## 2️⃣ Что такое нулевой указатель

**Нулевой указатель** — это указатель, который **не указывает ни на один объект в памяти**.

Проще:

> «указатель ни на что не указывает».

---

## 3️⃣ Зачем нужен нулевой указатель

Нулевой указатель используется для:

- обозначения «отсутствия объекта»
    
- безопасной инициализации указателей
    
- проверки перед разыменованием
    
- сигнализации об ошибках
    

---

## 4️⃣ `nullptr` — правильный способ (C++11+)

```cpp
int* p = nullptr;
```

### Почему именно `nullptr`

- имеет собственный тип `std::nullptr_t`
    
- неявно приводится к любому типу указателя
    
- **не приводится к целым типам**
    

---

## 5️⃣ Проверка нулевого указателя

```cpp
if (p == nullptr) {
    // указатель пуст
}
```

или короче:

```cpp
if (!p) { }
```

---

## 6️⃣ Почему не `0` и не `NULL`

### ❌ `0`

```cpp
int* p = 0;
```

- работает, но это **целочисленный литерал**
    
- может вызывать путаницу
    

---

### ❌ `NULL`

```cpp
int* p = NULL;
```

- макрос
    
- может быть `0` или `0L`
    
- небезопасен в перегрузках функций
    

---

### Пример проблемы:

```cpp
void f(int);
void f(int*);

f(NULL); // ❌ неоднозначность
f(nullptr); // ✔ вызывает f(int*)
```

---

## 7️⃣ Разыменование нулевого указателя

```cpp
int* p = nullptr;
*p = 10; // ❌ неопределённое поведение
```

❗ Всегда проверяй указатель перед разыменованием.

---

## 8️⃣ Нулевой указатель и арифметика

```cpp
int* p = nullptr;
p++; // ❌ UB
```

Арифметика с `nullptr` **запрещена**.

---

## 9️⃣ Возврат `nullptr` из функции

```cpp
int* find(int* arr, int size, int value) {
    for (int i = 0; i < size; ++i)
        if (arr[i] == value)
            return &arr[i];
    return nullptr;
}
```

---

## 🔟 Отличие от «висячего» указателя

|Тип указателя|Что означает|
|---|---|
|`nullptr`|ни на что не указывает|
|dangling pointer|указывает на уничтоженный объект|

---

## 1️⃣1️⃣ Частые ошибки 

❌ разыменовывают `nullptr`  
❌ сравнивают указатель с `0` вместо `nullptr`  
❌ используют `NULL` в современном C++  
❌ делают арифметику с `nullptr`

---

## 1️⃣2️⃣ Экзаменационный акцент


✔ объяснить, что такое `nullptr`  
✔ отличить его от `0` и `NULL`  
✔ проверять указатели  
✔ понимать опасность разыменования

---

## 1️⃣3️⃣ Итог

- `nullptr` — единственный правильный null pointer в C++
    
- безопаснее и выразительнее `0` и `NULL`
    
- всегда проверяй перед использованием
    
