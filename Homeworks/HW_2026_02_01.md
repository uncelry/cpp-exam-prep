## 🧩 Задача 1. Инварианты класса и защита данных

### **Условие**

Создай класс `Temperature`, который хранит температуру в градусах Цельсия.

Требования:

* температура не может быть ниже `-273` (абсолютный ноль)
* значение должно быть `private`
* конструктор принимает начальное значение
* метод `set()` меняет температуру
* метод `get()` возвращает значение
* при попытке установить некорректное значение — температура не меняется

---

## 🧩 Задача 2. Конструкторы + списки инициализации + const-поля

### **Условие**

Создай класс `Book` со следующими полями:

* `const int id`
* `std::string title`
* `int pages`

Требования:

* использовать **список инициализации**
* запретить изменение `id`
* добавить метод `print()`

---

## 🧩 Задача 3. Счетчик объектов (static + конструкторы + деструкторы)

### **Условие**

Создай класс `ObjectCounter`, который:

* считает количество **живых объектов**
* увеличивает счётчик в конструкторе
* уменьшает в деструкторе
* имеет `static` метод `getCount()`

---

## 🧩 Задача 4. Класс с ресурсом (подготовка к RAII)

### **Условие**

Создай класс `IntArray`, который:

* хранит динамический массив `int`
* размер задаётся в конструкторе
* память освобождается в деструкторе
* есть метод `set(index, value)`
* есть метод `get(index)`


---

## 🧩 Задача 5. Композиция классов

### **Условие**

Создай:

* класс `Engine` (мощность)
* класс `Car`, который **содержит Engine** (не наследует, а просто содержит одним из полей класса)
* `Car` должен:

  * инициализировать `Engine` через список инициализации
  * иметь метод `print()`
