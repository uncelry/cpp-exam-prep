Разбираем тему подробно.

---

## 🔹 What is a static array? How is it declared and initialized?

### 🔸 Перевод темы

**Что такое статический массив? Как его объявлять и инициализировать?**

---

## 1️⃣ Определение

**Статический массив** — это массив с **фиксированным размером**, который известен на этапе компиляции.

- Размер массива **не меняется** во время выполнения программы
    
- Память для него **выделяется в стеке** (stack) или в сегменте данных (для глобальных/статических массивов)
    
- Индексация начинается с нуля
    

---

## 2️⃣ Объявление статического массива

```cpp
int arr[5]; // массив из 5 элементов типа int
```

- `int` — тип элементов
    
- `[5]` — размер массива
    
- Элементы **не инициализированы** (для локальных массивов)
    

---

### 🔹 С инициализацией

```cpp
int arr[5] = {1, 2, 3, 4, 5};
```

- Элементы массива получают указанные значения
    
- Если указано меньше значений, оставшиеся инициализируются **нулём**
    

```cpp
int arr[5] = {1, 2}; // arr = {1, 2, 0, 0, 0}
```

---

### 🔹 Объявление без явного размера

```cpp
int arr[] = {1, 2, 3, 4, 5}; // компилятор сам определит размер = 5
```

---

### 🔹 Для других типов

```cpp
char letters[4] = {'a', 'b', 'c', 'd'};
double numbers[3] = {0.1, 0.2, 0.3};
```

---

## 3️⃣ Особенности статических массивов

1. **Фиксированный размер** — нельзя изменить после компиляции
    
2. **Быстрый доступ** к элементам по индексу `arr[i]`
    
3. **Память выделяется на стеке** для локальных массивов
    
4. **Инициализация**
    
    - Локальный массив без инициализации содержит **«мусор»**
        
    - Глобальный/статический массив без инициализации автоматически инициализируется **нулём**
        

---

## 4️⃣ Пример с доступом к элементам

```cpp
#include <iostream>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};

    for (int i = 0; i < 5; ++i) {
        std::cout << "arr[" << i << "] = " << arr[i] << std::endl;
    }

    return 0;
}
```

Вывод:

```
arr[0] = 10
arr[1] = 20
arr[2] = 30
arr[3] = 40
arr[4] = 50
```

---

## 5️⃣ Отличие от динамического массива

|Критерий|Статический массив|Динамический массив|
|---|---|---|
|Размер|фиксированный|может меняться во время выполнения|
|Память|стек (локальные), сегмент данных (глобальные/статические)|куча (heap)|
|Инициализация|локальные — мусор, глобальные — 0|нужно выделять вручную через `new`|
|Синтаксис|`int arr[5];`|`int* arr = new int[n];`|

---

## 6️⃣ Резюме для студента

- **Статический массив** — это массив фиксированного размера, известного на этапе компиляции
    
- Объявляется через квадратные скобки `[size]`
    
- Инициализируется через `{...}`
    
- Быстро и удобно для небольших массивов
    
- Используется, когда **размер известен заранее и не меняется**
    
