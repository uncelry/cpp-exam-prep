Разбираем тему подробно.

---

## 🔹 References and const

### 🔸 Перевод темы

**Ссылки и `const` в C++**

---

## 1️⃣ Что такое const-ссылка

**Const-ссылка** — это ссылка, через которую **нельзя изменять объект**, на который она ссылается.

```cpp
int x = 10;
const int& r = x;
```

- `r` нельзя использовать для изменения `x`
    
- Но `x` можно изменять напрямую
    

```cpp
x = 20;      // ✔
r = 30;      // ❌ ошибка компиляции
```

---

## 2️⃣ Зачем нужны const-ссылки

### 🔹 Передача больших объектов без копирования

```cpp
void print(const std::string& s) {
    std::cout << s << std::endl;
}
```

- Нет копирования объекта `s`
    
- Нельзя изменить `s` внутри функции
    
- Эффективно и безопасно
    

---

### 🔹 Привязка к временным объектам

```cpp
const int& r = 42; // допустимо
```

- Обычная `int&` не может ссылаться на rvalue (временный объект)
    
- `const int&` — может, компилятор создаёт временный объект в памяти
    

---

### 🔹 Защита данных от изменения

```cpp
void f(const int& x) {
    // x = 5; // ❌ нельзя
}
```

- Гарантия, что функция **не изменит объект**
    
- Особенно важно для API и библиотек
    

---

## 3️⃣ Правила работы const-ссылок

1. **Инициализация обязательна**
    

```cpp
const int& r; // ❌ ошибка
```

2. **Нельзя изменять объект через ссылку**
    

```cpp
const int& r = x;
r = 20; // ❌ ошибка
```

3. **Можно привязывать к временным объектам**
    

```cpp
const std::string& s = std::string("hello"); // ✔
```

4. **Можно использовать для передачи аргументов функции**
    

```cpp
void print(const std::vector<int>& v);
```

5. **Можно использовать для возврата значений**
    

```cpp
const int& max(const int& a, const int& b) {
    return (a > b) ? a : b;
}
```

- Возвращаемая const-ссылка гарантирует, что вызов не изменит объект
    

---

## 4️⃣ Сравнение с обычной ссылкой

|Критерий|Ссылка (`int&`)|Const-ссылка (`const int&`)|
|---|---|---|
|Можно изменить объект|✔|❌ через ссылку|
|Можно привязать к rvalue|❌|✔|
|Эффективность|✔|✔|
|Безопасность|меньше|больше|

---

## 5️⃣ Пример с функцией

```cpp
#include <iostream>
#include <string>

void printString(const std::string& s) {
    std::cout << s << std::endl;
}

int main() {
    std::string name = "Alice";
    printString(name);          // безопасно
    printString("Bob");         // временный объект, тоже безопасно
}
```

- Здесь нет копирования строки
    
- Функция не может изменить переданный объект
    
