**Оператор `switch` в C++**

---

## 1️⃣ Перевод темы

**The switch statement → Оператор `switch`**  
**When should it be used? → Когда его следует использовать?**  
**Examples → Примеры**  
**Initialization in switch → Инициализация в `switch`**  
**The `[[fallthrough]]` attribute → Атрибут `[[fallthrough]]`**

---

## 2️⃣ Что такое `switch`

`switch` — это оператор выбора, который позволяет:

- выполнить **одну из нескольких веток**
    
- на основе **значения выражения**
    

Используется, когда есть **много вариантов** для **одного значения**.

---

## 3️⃣ Общий синтаксис `switch`

```cpp
switch (expression) {
case value1:
    // код
    break;
case value2:
    // код
    break;
default:
    // код
}
```

---

## 4️⃣ Когда следует использовать `switch`

✔ когда сравнение идёт **по одному значению**  
✔ когда много вариантов (`if-else` становится громоздким)  
✔ при работе с `enum`  
✔ когда важна читаемость и производительность

❌ для диапазонов  
❌ для сложных логических условий

---

## 5️⃣ Тип выражения в `switch`

Выражение должно быть:

- целочисленным типом
    
- `char`
    
- `enum`
    

❌ нельзя:

```cpp
switch (3.14) { } // ошибка
```

---

## 6️⃣ Простой пример

```cpp
int day = 3;

switch (day) {
case 1:
    std::cout << "Monday";
    break;
case 2:
    std::cout << "Tuesday";
    break;
case 3:
    std::cout << "Wednesday";
    break;
default:
    std::cout << "Unknown day";
}
```

---

## 7️⃣ `default`

Выполняется, если ни один `case` не совпал.

```cpp
default:
    std::cout << "Invalid input";
```

---

## 8️⃣ Инициализация в `switch` (C++17+)

Можно объявить переменную прямо в `switch`.

```cpp
switch (int x = getValue(); x) {
case 1:
    std::cout << "One";
    break;
case 2:
    std::cout << "Two";
    break;
}
```

📌 Область видимости `x` — весь `switch`.

---

## 9️⃣ `fallthrough` (проваливание)

### 🔹 Что это такое

Если **нет `break`**, выполнение переходит в следующий `case`.

```cpp
switch (x) {
case 1:
    std::cout << "One";
case 2:
    std::cout << "Two";
}
```

Вывод при `x == 1`:

```
OneTwo
```

---

## 🔟 Атрибут `[[fallthrough]]`

### 🔹 Назначение

Явно указывает компилятору:

> «проваливание сделано намеренно»

---

### 🔹 Пример

```cpp
switch (x) {
case 1:
    std::cout << "One\n";
    [[fallthrough]];
case 2:
    std::cout << "Two\n";
    break;
}
```

✔ убирает предупреждения  
✔ улучшает читаемость

---

## 1️⃣1️⃣ Область видимости в `case`

⚠️ Ошибка:

```cpp
case 1:
    int x = 5; // ошибка
    break;
```

✔ Правильно:

```cpp
case 1: {
    int x = 5;
    break;
}
```

---

## 1️⃣2️⃣ Несколько `case` для одного блока

```cpp
switch (x) {
case 1:
case 2:
case 3:
    std::cout << "1–3";
    break;
}
```

---

## 1️⃣3️⃣ Частые ошибки

❌ забывают `break`  
❌ используют `switch` вместо `if` с диапазонами  
❌ не знают про `[[fallthrough]]`  
❌ объявляют переменные без `{}`

---

## 1️⃣4️⃣ Итог

Надо понимать:

✔ назначение `switch`  
✔ когда его использовать  
✔ допустимые типы  
✔ механизм fallthrough  
✔ зачем нужен `[[fallthrough]]`  
✔ инициализацию в `switch`
